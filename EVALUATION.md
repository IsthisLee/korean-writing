# korean-writing 합격 기준

목적: **Claude Code가 쓰는 한국어 글의 품질을 맡는다.** 글 작성 요청은 스킬이 처음부터 맡고 이미 쓴 글은 윤문 스킬이 다듬고 README는 절 구성을 잡고 `.md` 편집은 훅이 검사한다. 평소 답변과 서브에이전트에 규칙을 넣던 주입은 2026-09-11 에 뺐다(H13). 그 주입을 잰 C5·G·H·N 절은 뺀 날까지의 기록으로 남긴다.

이 목적이 달성됐는지 판정하는 기준이다. 하나라도 미달이면 고친다.

판정의 근거가 어디에 있는지는 [docs/foundations.md](docs/foundations.md)에 모아 두었다. 연구 문헌, 분류 체계, 이 저장소가 만든 규칙, 재현 장비를 한자리에서 가리킨다.

## A. 훅 정확도

정답 데이터는 **이 세션에서 내가 실제로 쓴 어색한 문장 10건**(`ground-truth.json`)과 **같은 맥락의 정상 문장 5건**(`clean.json`)이다. 합성 예문이 아니라 실측된 실패다.

| #   | 기준                                               | 임계      |
| --- | -------------------------------------------------- | --------- |
| A1  | 위반 문장 검출률                                   | **10/10** |
| A2  | 정상 문장 오탐                                     | **0/5**   |
| A3  | 실제 프로젝트 `.md` 편집 시 오탐                   | **0건**   |
| A4  | 분류 정확도 — 검출된 항목이 맞는 코드로 보고되는가 | **10/10** |

A2가 A1보다 중요하다. 게이트가 정상 작업을 막으면 사람이 게이트를 꺼버린다.

## B. 스킬 트리거

| #   | 기준                                                               | 임계    |
| --- | ------------------------------------------------------------------ | ------- |
| B1  | 이 세션에서 실제로 나온 글 작성 요청 5종에 description 이 걸리는가 | **5/5** |
| B2  | 코드 작업 요청에는 안 걸리는가                                     | **0/5** |

B1 대상: "슬랙 내 캐쥬얼하게 확인 요청드리려는데", "비개발자 눈높이로 정리해줄래", "노션용으로 정리", "커밋 메시지 써줘", "README 작성"

## C. 스킬 내용 유효성 — 핵심

**스킬을 로드하면 실제로 더 나은 글이 나오는가.** `superpowers:writing-skills`의 Iron Law 는 "스킬 없이 실패하는 것을 보지 않았으면 그 스킬이 옳은 것을 가르치는지 알 수 없다"이다.

| #   | 기준                                                                                | 임계         |
| --- | ----------------------------------------------------------------------------------- | ------------ |
| C1  | 정답 데이터 10건을 스킬 규칙으로 교정할 때, 각 위반에 대응하는 처방이 스킬에 있는가 | **10/10**    |
| C2  | 교정 결과가 사실을 바꾸지 않는가 (수치·고유명사 보존)                               | **10/10**    |
| C3  | 스킬 문서 자신이 자기 규칙을 지키는가                                               | 위반 **0건** |

**C4 스킬 유무 비교 (2026-09-10).** 같은 프롬프트 4개(슬랙 안내문, QA 보고서, 배포 절차 문서, 팀 규칙 문서)를 `claude -p` 로 플러그인이 로드된 기본 설정과 사용자 설정을 뺀 상태(`--setting-sources project`)로 각각 돌렸다. 모델은 claude-opus-5. 유효 표본 7건(1건은 max-turns 초과로 빈 결과)이 전부 훅을 통과했고 줄표·첫째둘째·관용구·승패·것들 표지가 0이었다. 이 모델과 이 표본으로는 스킬의 생성 단계 효과를 구분하지 못했다. 실측 문서의 줄표 34~~207개는 2026년 6~~8월 산출물이라 그때의 모델과 설정에서 나온 것이다. 훅은 모델과 설정이 바뀌어도 남는 안전망이고 스킬의 효과는 표지가 나오는 모델·프롬프트에서 다시 재야 한다.

**C5 상시 규칙 주입 (2026-09-10).** 스킬이 뜨지 않는 평소 답변에서 `plugin/hooks-handlers/always-on.md` 주입이 문체를 개선하는지 쟀다. 기준을 실행 전에 적어 두고 결과를 보고 바꾸지 않았다. 프롬프트 12개(함수 설명, 에러 진단, diff 설명, 설계 추천, 리팩터링, 테스트 실패, 성능, 개념 설명, 로그 분석, PR 리뷰, 설계 점검, git 충돌)에 조건 2개와 표본 2개로 48건을 생성하고 같은 모델에 순서를 바꿔 두 번씩 블라인드로 판정했다(48회). 모델은 claude-fable-5-1, effort xhigh.

| 측정                              | 기준선  | 주입    |
| --------------------------------- | ------- | ------- |
| 블라인드 쌍대 승 (24쌍)           | 0       | **21**  |
| 무승부                            | 3       | 3       |
| 훅 검사기 적중                    | 2       | **0**   |
| 정규식 원시 패턴 합계             | 5       | **0**   |
| 굵게 `**` 사용                    | 174     | **110** |
| 반말 종결 (재확인 6표본)          | 8       | **0**   |
| 기술 오류 (문체를 뺀 별도 검사)   | 34      | 32      |

12개 프롬프트 전부에서 주입 쪽이 이기거나 비겼다. 판정이 기준선에서 반복해 지적한 것은 굵은 소제목 남발, `결론부터`·`정리하면` 같은 상투 문두, 영어 직역(`화면을 얼린다`, `배치는 굶습니다`), 형식명사, 문체 혼용이다. 사실 정확도는 심각한 오류가 양쪽 0건이고 사소한 과장은 34 대 32 로 사실상 같다. 길이 비율의 중앙값은 1.03 이다.

주입문의 높임은 따로 고쳤다. 초안이 해라체로 적혀 있어 답변까지 해라체로 기울었고 `상대에게 맞춘다` 로 적은 판은 반말 질문에 반말로 답해 더 나빴다(6표본에서 반말 종결 18회). `평소대로 존댓말로 답한다` 로 고정하니 0회가 됐다.

**한계.** 판정 모델과 생성 모델이 같다. 표본이 2다. 단일 턴이라 긴 대화에서 효과가 약해지는지는 재지 않았다. 정규식 훅은 평소 답변에서 표본당 0.21건만 잡으므로, 이 검사기로는 답변 품질의 회귀를 감시하지 못한다.

**C6 규칙집 커버리지 (2026-09-10 신설).** 스킬이 im-not-ai 규칙집을 얼마나 덮는가. 그 규칙집의 `quick: true` 항목 61개는 표층 신호만으로 판정할 수 있어 생성 단계에 그대로 옮길 수 있다. 항목마다 고유 표지를 정해 `SKILL.md` 에서 찾는 방식으로 셌다.

| 시점 | quick 항목 처방 보유 | 비고 |
| --- | --- | --- |
| 이번 개정 전 | 61개 중 **21개** | S1 가운데 A-3·A-16·C-8·C-11·D-3 이 통째로 빠져 있었다 |
| 이번 개정 후 | 61개 중 **60개** | 빠진 하나는 G-3. 규칙집이 "실증 부족 — hold" 로 표시한 항목이라 뺐다 |

빠져 있던 것 가운데 둘이 그 규칙집의 실측 최강 신호다. C-8 부정 대구는 밀도가 사람 대비 9.2배(개인 블로그 대비 18배)이고 세 모델과 세 과업 조건에서 모두 재현된 유일한 항목이다. C-11 연결어미 뒤 쉼표는 KatFish(ACL 2025) 에세이 측정에서 사람 4.10% 대 AI 19.83% 로 단일 지표 분리도가 가장 크다.

**C7 생성 단계 비교 (2026-09-10).** 규칙을 늘린 것이 실제로 생성물을 바꾸는지 쟀다. 프롬프트 넷(배포 절차 공지, 실패율 보고서, 릴리스 노트, 코드 리뷰 문화 문서)에 조건과 표본 다섯으로 생성했다. `claude -p --append-system-prompt "$(cat SKILL.md)" --strict-mcp-config --setting-sources ""`, 모델은 claude-opus-5.

조건은 넷이다. 규칙 없음, 옛 스킬, 새 스킬 초판, 새 스킬 최종판이다. 초판과 최종판을 나눈 이유는 아래에 있다.

| 측정 | 규칙 없음 | 옛 스킬 | 새 스킬 초판 | 새 스킬 최종판 |
| --- | --- | --- | --- | --- |
| 표본당 AI 티 표지 | 0.95 | 1.80 | **0.25** | 0.90 |
| 연결어미 쉼표 비율 평균 | 18.9% | 38.8% | **1.9%** | 11.1% |
| 문장 길이 표준편차 | 16.16 | 16.28 | 12.93 | **18.97** |
| 최장 문장 길이 | 68.8 | 70.3 | 60.9 | **81.1** |
| 100자 넘는 문장 (표본당) | 0.05 | 0.00 | 0.00 | **0.10** |

**정규식 계수만으로는 품질을 말할 수 없다.** 초판은 표지를 1.80에서 0.25로 줄였는데, 저장소의 기존 판정 기준(`docs/experiments/always-on/judge_one.sh` 의 감점 목록)으로 블라인드 쌍대 판정을 돌리니 옛 스킬과 구별되지 않았다. 20쌍을 순서를 바꿔 두 번씩 물어 일관되게 갈린 것이 초판 5승 옛 스킬 6승이고 아홉 쌍은 순서에 따라 답이 뒤집혔다. 판정 기준에는 이번에 넣은 대구·쉼표 항목이 없다. 자기가 만든 지표만 좋아졌는지 보려고 일부러 그대로 뒀고, 결과는 그랬다.

**판정자가 되풀이해 지적한 것은 리듬이었다.** "문장 길이가 고르게 짧아 기계적으로 읽힌다", "글 2는 문장 길이도 고르지 않아 더 자연스럽다" 같은 문장이 반복해 나왔다. 재 보니 문장 길이 표준편차가 16.28에서 12.93으로 떨어져 있었다. 규칙을 지키려고 문장을 짧게 끊은 것이고, 길이 균일성은 규칙집의 E-1 항목이다. 하나를 고치면서 다른 하나를 만든 셈이다.

그래서 `SKILL.md` 에 「리듬」 절을 넣었다. 문단마다 100자 안팎의 긴 문장을 하나 두라는 것과, 규칙은 표현을 빼라는 것이지 문장을 자르라는 뜻은 아니라는 것 둘이다. 최종판은 표준편차 18.97로 네 조건 중 가장 높다. 연결어미 쉼표는 1.9%에서 11.1%로 올랐는데, 사람이 쓴 글의 이 머신 실측 중앙값이 약 15% 이므로 1.9%는 오히려 사람보다 적었다. 목표는 0이 아니고 사람 수준이다.

**블라인드 쌍대 판정.** 생성은 claude-opus-5, 판정은 claude-fable-5-1 이다. 판정자와 생성자를 다른 모델로 뒀다(C5 의 한계였던 부분이다). 쌍마다 순서를 바꿔 두 번 묻고, 두 번 같은 답이 나온 쌍만 승패로 셌다.

| 비교 | 일관 승패 | 이항 단측 p |
| --- | --- | --- |
| 최종판 대 옛 스킬 | **12승 4패** (갈림 4) | **0.038** |
| 최종판 대 초판 | 10승 5패 (갈림 5) | 0.151 |
| 초판 대 옛 스킬 | 5승 6패 (갈림 9) | 0.726 |

최종판이 이 저장소의 종전 스킬을 이긴 것은 유의하다. 초판은 이기지 못했다. 리듬 절이 그 차이를 만들었다는 것이 최종판 대 초판 비교인데 방향만 맞고 유의하지는 않다.

**한계.** 모델 하나로 생성했고 프롬프트가 여덟이다. 판정자를 사람이 아닌 모델이 맡았다. 스킬을 시스템 프롬프트로 붙여 돌렸으므로 실제 스킬 로드 경로와 완전히 같지는 않다. 갈림 쌍이 20~45% 나오는데 이는 두 글의 차이가 작다는 뜻이기도 하다.

**옛 스킬이 규칙 없음보다 나빴다.** 연결어미 쉼표가 32 대 14 이고 비율로는 38.8% 대 18.9% 다. 왜 그런지는 확인하지 못했다. 옛 스킬 본문의 쉼표 비율 자체는 10% 로 낮으므로 본문 흉내는 설명이 되지 않는다. 줄표 처방이 "쉼표나 문장 분리로 바꾼다" 라 쉼표 쪽으로 민다는 가설이 있으나, 새 스킬도 같은 문장을 그대로 두고 쉼표가 떨어졌으므로 그 가설만으로는 부족하다.

**C9 im-not-ai 수준에 닿았는가 (2026-09-10).** 이 플러그인의 목표는 스킬의 품질이 im-not-ai 수준이 되거나 그보다 높은 것이다. 그쪽은 이미 쓴 글을 고치는 파이프라인이므로, 비교는 「규칙 없이 쓴 글을 im-not-ai 로 윤문한 것」 대 「스킬로 처음부터 쓴 글」로 세웠다.

**두 조건에서 쟀다.** 단문은 400~600자 업무 문서 여덟 종(공지·보고서·릴리스 노트·문화 문서·장애 공지·회의록·기획안·가격 안내)이고, 장문은 1,500자 칼럼·에세이·리포트 여섯 종이다. 장문을 따로 둔 이유는 윤문 파이프라인이 원래 강한 자리가 그쪽이기 때문이다.

**단문에서는 Fast Path 한 콜, 장문에서는 정밀 3콜을 재현했다.** 정밀 모드는 진단·윤문·마무리 세 콜이고, 결합 입력은 저장소의 실제 스크립트 `plugin/scripts/prepare_monolith_input.py` 가 만들었다. 시스템 프롬프트는 `plugin/agents/humanize-diagnostician.md`·`humanize-monolith.md`·`humanize-finalizer.md` 와 규칙집 원문이다. 도구가 없는 세션이라 파일 입출력만 표준입출력으로 바꿨다.

| 윤문 입력 | 경로 | 변경률 중앙값 | 거의 안 고침 |
| --- | --- | --- | --- |
| 규칙 없이 쓴 단문 | Fast Path 1콜 | 0.3% | 20건 중 13 |
| 규칙 없이 쓴 장문 | 정밀 3콜 | 1.0% | 24건 중 13 |

**진단을 붙여도 변경률이 오르지 않았다.** 그쪽 문서는 진단 없이 윤문만 하면 0.5%로 no-op 이 되고 진단을 붙이면 11%로 뛴다고 적고 있는데, 여기서는 1.0% 였다. 재현이 잘못됐는지 보려고 진단 산출물을 열어 봤다. 5KB 짜리 진단이 taxonomy ID 와 정량 앵커를 달고 나왔고 shim 이 결합 입력 앞머리에 실었다. 재현은 맞았다. 진단이 스스로 내린 결론이 "처방 없음" 이었다. 한 건의 진단은 `C-11 0건·C-12 0건은 사람 글 방향의 강한 증거` 라고 적었다. **claude-opus-5 가 2026년에 쓰는 한국어가 이미 그 규칙집이 겨냥하는 상태에서 벗어나 있다는 뜻으로 읽는다.**

**표지 밀도 (장문, 1000자당).**

| 항목 | 규칙 없음 | 스킬 | im-not-ai 윤문본 |
| --- | --- | --- | --- |
| C-8 대구 | 0.42 | **0.22** | 0.39 |
| C-11 연결어미 쉼표 | 0.69 | 0.73 | **0.60** |
| I-1·3 것 구문 | 0.14 | **0.04** | 0.14 |
| F-5 ~적 N | 0.21 | 0.29 | **0.21** |
| 합계 | 1.49 | **1.32** | 1.37 |
| 문장 길이 표준편차 | 21.42 | 20.81 | 19.05 |

**1차 판정은 동등에서 멈췄다.** 단문 32쌍은 claude-fable-5-1 이, 장문 24쌍은 claude-opus-5 가 판정했다. 쌍마다 순서를 바꿔 두 번 묻고 두 번 같은 답이 나온 쌍만 셌다. 결과는 41쌍에서 스킬 22승 im-not-ai 19승이었다(단측 p=0.378). 우열을 말할 수 없는 자리다.

**진 쌍의 이유를 읽어 세 가지를 고쳤다.** 판정자가 남긴 문장이 세 갈래로 모였고 셋 다 실측으로 확인됐다.

| 판정자가 지적한 것 | 실측 | 규칙에 있었나 |
| --- | --- | --- |
| 말미의 분량 보고와 후속 제안 | 표본당 0.29·0.42회 | 없었다 |
| 소제목으로 잘게 나눔 | 헤딩 표본당 1.21줄 (규칙 없음·im-not-ai 는 0.62) | 없었다 |
| 문단마다 비슷한 리듬으로 끝남 | 문단 끝 문장 길이 표준편차 14.58 (규칙 없음 18.13, im-not-ai 16.24) | 문서 전체 리듬만 있었다 |

문서 전체의 문장 길이 표준편차는 스킬이 20.94로 im-not-ai 의 19.22보다 높았다. 고른 것은 문단을 닫는 문장이었고 그 축을 보지 않으면 안 보인다. `SKILL.md` 에 「요청한 글만 낸다」 절을 새로 넣고, 산문에 소제목을 달지 않는 규칙과 문단 끝 문장 길이를 돌려 쓰는 규칙을 더했다.

| 측정 | 규칙 없음 | 1차 스킬 | **고친 스킬** | im-not-ai |
| --- | --- | --- | --- | --- |
| 분량 보고 (표본당) | 0.50 | 0.29 | **0.00** | 0.38 |
| 후속 제안 (표본당) | 0.54 | 0.42 | **0.00** | 0.38 |
| 헤딩 (표본당) | 0.62 | 1.21 | **0.00** | 0.62 |
| 수평선 (표본당) | 1.58 | 1.62 | **0.04** | 1.38 |
| 문단 끝 길이 표준편차 | 18.13 | 14.58 | **16.67** | 16.24 |

**2차 판정에서 우위가 확인됐다.** 같은 프롬프트와 같은 im-not-ai 산출물에 고친 스킬을 붙였다. 판정은 claude-opus-5, 순서를 바꿔 두 번씩이다.

| 조건 | 스킬 승 | im-not-ai 승 | 갈림 | 이항 단측 p |
| --- | --- | --- | --- | --- |
| 단문 32쌍 | **28** | 0 | 4 | 0.0000 |
| 장문 24쌍 | **21** | 0 | 3 | 0.0000 |
| 합산 56쌍 | **49** | 0 | 7 | 2 의 -49 제곱 |

**마무리 문구 덕이 아니다.** 장문에서 im-not-ai 쪽에만 어시스턴트 마무리가 남아 있었으므로(24건 중 14건, 스킬은 0건) 그것을 걷어내고 다시 붙였다. 20개 파일에서 머리말·분량 보고·후속 제안·수평선을 지웠고 본문은 건드리지 않았다. 결과는 **18승 0패, 갈림 6** 이다. 단문에서는 애초에 스킬 쪽에 마무리 문구가 더 많았는데도(32건 중 9건 대 7건) 28승 0패였다.

**목표 문장에 비춘 결론.** "im-not-ai 수준이 되거나 그보다 높은 수준" 에서 **그보다 높은 쪽이다.** 56쌍에서 진 쌍이 없다.

**한계.** 생성 모델이 claude-opus-5 하나다. 판정자를 사람이 아닌 모델이 맡았고 2차 판정은 생성 모델과 같은 opus-5 다(1차의 fable-5-1 은 계정 사용량 소진으로 48회 중 34회가 실패했다). 다만 두 조건 모두 같은 모델이 쓴 글이므로 어느 한쪽으로 기울 자리는 아니다. 56쌍 중 7쌍은 순서에 따라 답이 뒤집혔다. 윤문 파이프라인 재현에서 오케스트레이터의 게이트 스크립트를 붙이지 않았는데, 변경률이 최대 13% 라 그 게이트가 발동할 자리가 없었다.

**C10 장문에서 드러난 결함 둘 (2026-09-10).** 단문 실험만 했으면 못 봤을 것이다.

**요청 분량을 지키지 않았다.** 1,500자를 요청한 장문 24건에서 규칙 없이 쓴 글의 한글이 중앙값 1,205자인데 규칙을 붙인 글은 1,128자였고 1,200자를 넘긴 것이 12건 대 8건이었다. 규칙이 길어지면서 할 말을 줄인 것으로 본다. `SKILL.md` 에 「요청받은 분량을 지킨다」를 넣어 1,165자가 됐다. 여전히 규칙 없는 쪽보다 짧다.

**`것` 구문이 오히려 늘었다.** 1000자당 0.14에서 0.53으로 올랐다. `~라는 것이다`·`~다는 것이다` 로 문단을 닫는 자리였고, 규칙에 예문이 하나뿐이라 그 자리를 못 짚었다. 실제로 나온 문장을 예문으로 넣으니 0.04가 됐다.

**요청 분량은 아직 남았다.** 소제목과 마무리 안내를 걷어내니 한글 중앙값이 1,165자에서 945자로 더 떨어졌다. 1,500자를 요청한 자리에서 규칙 없이 쓴 글은 1,205자, im-not-ai 윤문본은 1,209자다. 판정 기준에 길이가 없어 승패에는 영향이 없었지만, 요청한 분량을 안 지키는 것은 그 자체로 결함이다.

「요청받은 분량은 본문으로 채운다」 를 「요청한 글만 낸다」 절에 넣는 판을 써 봤으나 **판정으로 검증하지 못했다.** 그 시점에 계정 세션 한도에 걸렸다. 측정한 판과 배포하는 판이 달라지면 위의 49승 0패가 무엇에 대한 숫자인지 흐려지므로 그 편집을 되돌렸다. 그때 저장소에 있던 `SKILL.md` 가 49승 0패를 낸 바로 그 판이다. 2026-09-11 에 문체 따르기 절을 넣은 판은 기본 네 프롬프트로 다시 붙여 3승 1패였다(J 절). 다음에 할 일은 그 문구를 다시 넣고 `docs/experiments/skill-vs-imnotai/run.sh` 를 장문 프롬프트로 돌려 분량이 오르는지, 승률이 그대로인지 함께 보는 것이다.

**`것` 구문도 되돌아왔다.** 1000자당 0.04에서 0.22로 올랐다(im-not-ai 0.14). 소제목을 빼고 문단으로 이어 쓰면서 설명을 닫는 자리가 늘어난 것으로 본다. 확인하지 않은 추정이다.

**측정에서 두 번 틀렸다.** 처음 분량을 잴 때 빈 파일 넷을 중앙값에 넣어 991자로 보고했다. 빼면 1,078자다. 빈 파일은 `xargs` 로 병렬 실행할 때 `claude` 가 작업 목록 stdin 을 물어 마지막 묶음이 통째로 실패한 것이고, `< /dev/null` 로 끊으니 넷 다 1,193~1,354자로 나왔다. 판정 96건 가운데 35건이 파싱에 실패한 것도 계정 사용량 소진 메시지가 JSON 자리에 들어온 것이었다.

## D. 구조 건전성

| #   | 기준                                           | 임계                      |
| --- | ---------------------------------------------- | ------------------------- |
| D1  | 외부 네트워크 호출                             | **0건**                   |
| D2  | 상시 컨텍스트 비용                             | **1,600 토큰 이하**       |
| D3  | 회귀 테스트가 실제 결함을 잡는가 (변이 테스트) | 주입한 결함 **전부 검출** |
| D4  | 플러그인이 로드되고 스킬 6종(이 저장소 3, im-not-ai 내장 3)과 에이전트 3종과 명령 1종이 인식되는가 | **로드됨**                |
| D5  | ~~규칙 주입이 세션 시작이나 서브에이전트 실행을 막지 않는가~~ | 주입을 뺐다(H13)          |
| D6  | `hooks.json` 이 가리키는 핸들러가 실제로 있고 실행되는가 | **전부 존재·실행 가능** (CI)  |

## E. 실패 모드

| #   | 기준                                                             |
| --- | ---------------------------------------------------------------- |
| E1  | 훅이 죽어도 편집이 막히지 않는다 (잘못된 입력·빈 입력·거대 파일) |
| E2  | 격식 문서(계약·약관)에 대해 예외가 명시돼 있다                   |
| E3  | 오탐이 났을 때 사람이 무시할 경로가 있다                         |

---

## F. 사용하는 사람의 기준 (2026-09-10 신설)

설치해서 1년 넘게 쓰는 사람의 자리에서 본 기준이다. 검사기는 정확해도 쓰기 불편하면 꺼진다.

| #   | 기준                                                                                          | 상태                                                                                                                                                                        |
| --- | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | 설치가 명령 두 줄로 끝나고 설정할 것이 없는가                                                 | 통과. 2026-09-10 공개 저장소 생성 후 격리 HOME 에서 `claude plugin marketplace add IsthisLee/claude-korean-writing`(잰 당시 저장소 이름) 과 `install` 을 실행했고 설치본의 훅이 실제로 검사한다 |
| F2  | 파일 하나, 세션 하나, 플러그인 전체 단위로 끌 수 있는가                                       | 통과. 파일 머리 표시, `KOREAN_WRITING_HOOK_DISABLED=1`, `claude plugin disable`                                                                                             |
| F3  | 이미 써 둔 문서와 CI 에서 훅과 같은 검사를 돌릴 수 있는가                                     | 통과. `plugin/scripts/check.sh`. GitHub Actions 첫 실행 성공(2026-09-10)                                                                                                           |
| F4  | 저장소의 산문 문서 전부가 자기 훅을 통과하는가 (검사기가 자기 글을 못 지키면 사람이 무시한다) | 통과. 산문 문서 13종 통과. 2026-09-10 이전의 통과는 공허했다 — 아래 17번을 보라                                                                                                                   |
| F5  | 영어 문서 안의 한국어 문단도 잡는가                                                           | 통과. 한글 비중 30% 이상인 줄만 모아 재판정                                                                                                                                 |
| F6  | 실행 환경이 빠져도 작업을 막지 않는가                                                         | 통과. python3 가 없으면 검사 없이 통과                                                                                                                                      |
| F7  | 상시 컨텍스트 비용을 스킬 6종과 에이전트 3종 기준으로 다시 쟀는가                              | 통과. 격리 HOME 기준 스킬 설명 70·230·60·70 으로 약 430, 에이전트 102·107·88 로 297. 실제 HOME 에서는 에이전트가 773 으로 잡힌다(아래 D2 절)                                                 |

## G. 자리별 비용과 성능 (2026-09-11 신설)

> 이 절의 상시 규칙과 서브에이전트 주입 줄은 2026-09-11 H13 으로 두 주입을 빼기 전의 값이다. 뺀 뒤의 상시 비용은 D2 에 있다.

한국어가 나오는 다섯 자리를 한자리에 놓고 잴 수 있는 것만 쟀다. **품질 숫자는 자리마다 잣대가 달라 서로 줄 세울 수 없다.** 상시 규칙은 주입을 켠 답과 끈 답을 자기끼리 붙인 값이고, 스킬은 im-not-ai 윤문본과 붙인 값이며, 검사 훅은 검출률과 오탐률이다. 자리끼리 견줄 수 있는 것은 토큰과 시간뿐이다.

**측정 조건.** `claude -p --strict-mcp-config --max-turns 1 --output-format json` 의 `usage` 에서 `input_tokens` 와 `cache_creation_input_tokens` 와 `cache_read_input_tokens` 를 더했다. 사용자 설정은 빼지 않았다. `--setting-sources ""` 를 붙이면 심볼릭 링크로 로드된 이 플러그인의 SessionStart 훅이 돌지 않아 주입을 켠 실행과 끈 실행이 같은 값을 낸다. 기준선 프롬프트는 `1+1은?` 이고 총 26,515 다. 절대값은 세션 조건에 따라 달라지므로 차이만 본다.

### 상시로 드는 것

세션을 열기만 해도 들어가는 몫이다.

| 항목 | 토큰 | 어떻게 쟀나 |
| --- | --- | --- |
| 상시 규칙 주입 | **816** | 훅을 껐다 켜서 총량 차이를 봤다. 2026-09-11 에 3,552 대 4,368. 726 → 784(H5) → 816(H11) 으로 늘었다 |
| 스킬 설명 넷 | 약 430 | `/context` 의 스킬 표. korean-writing 70, humanize-korean 230, korean-character-count 60, crafting-effective-readmes 70. crafting-effective-readmes 는 v1.3.0 뒤에 뺐고 뺀 뒤로 다시 재지 않았다 |
| 에이전트 셋 | 773 | `/context` 의 Custom Agents 표. 266·273·234. 격리 HOME 에서는 297 로 잡힌다 (D2 절) |
| 서브에이전트 주입 | **816 × 서브에이전트 수** | 세션당 한 번이 아니라 서브에이전트를 띄운 수만큼 곱해진다. 상시 몫이 아니라 쓸 때 드는 몫이다. 끄는 스위치를 따로 뒀다 |
| 슬래시 전용 스킬 둘 | **0** | `humanize` 와 `humanize-redo` 는 `disable-model-invocation: true` 라 `/context` 의 스킬 표에 실리지 않는다. 이름을 쳐야만 뜨는 대신 상시 몫이 없다 |

옛 규칙 726 은 D2 절에 적힌 값이 그대로 재현된 것이었다. 재는 방법이 맞는지는 그 앞 판으로 662 가 나오는지 먼저 보면 안다.

### 요청할 때만 드는 것

스킬 본문을 프롬프트에 실어 기준선과의 차이를 봤다. 꼬리 문구 몫으로 15 토큰 안팎이 섞여 있으므로 끝자리는 믿지 않는다.

| 파일 | 토큰 | 바이트 | 언제 열리나 |
| --- | --- | --- | --- |
| `SKILL.md` | 약 10,100 | 21,481 | 글 작성 요청 |
| `plugin/skills/humanize-korean/SKILL.md` | 약 14,100 | 29,169 | 윤문 요청 |
| `plugin/skills/korean-character-count/SKILL.md` | 약 1,130 | 2,524 | 글자 수 요청 |
| `plugin/skills/crafting-effective-readmes/SKILL.md` | 약 940 | 2,676 | README 요청 |
| `plugin/hooks-handlers/always-on.md` | 816 | 1,802 | 세션마다 |

글 작성 요청 하나에 실제로 드는 것은 상시 몫 약 1,930 에 본문 10,100 을 더한 12,000 안팎이다. 윤문은 본문 14,100 이 콜 수만큼 곱해진다.

### 검사 훅의 지연

훅은 bash 안에서 python3 정규식을 돌린다. LLM 을 부르지 않으므로 토큰은 0 이고 남는 것은 지연뿐이다. 같은 입력을 10회씩 넣어 중앙값을 냈다.

| 편집분 | 중앙값 | 최소 | 최대 |
| --- | --- | --- | --- |
| 한글 730자 | 35.3ms | 33.2ms | 36.7ms |
| `SKILL.md` 전체 (10,102자) | 38.3ms | 36.2ms | 40.2ms |
| `README.md` 전체 (35,643자) | 42.5ms | 39.7ms | 47.7ms |

`plugin/scripts/check.sh --all` 로 문서 30종을 한 번에 검사하면 2.4초다(5회 중앙값). 파일마다 python3 를 새로 띄우므로 파일당 약 79ms 다. 훅 등록의 제한 시간은 10초인데 가장 큰 문서가 그 0.5% 도 쓰지 않는다.

### 윤문 두 경로

| 경로 | 입력 | 걸린 시간 | 비용 | 턴 | 변경률 | 등급 | 자체검증 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Fast 1콜 (light) | 배포 공지 280자 | **130초** | **0.64달러** | 11 | 1.1% | A | 6/6 |
| 정밀 3콜 (standard 승급) | AI 티를 몰아넣은 200자 | 451초 | 1.64달러 | 16 | 39.0% | A- | 6/6 |

Fast 경로는 2026-09-11 에 쟀다. 조건은 `claude -p "/korean-writing:humanize <본문>" --setting-sources "" --strict-mcp-config --plugin-dir <저장소> --dangerously-skip-permissions --max-turns 40` 이고 격리된 작업 폴더다. 정밀 3콜 기록은 [`docs/samples/humanize-run.md`](docs/samples/humanize-run.md) 에 있다.

**입력이 이미 깨끗하면 거의 손대지 않는다.** Fast 경로가 실제로 고친 것은 연결어미 뒤 쉼표 3개뿐이다. `~를 통해` 와 문두 `또한` 과 `~할 수 있다` 는 각각 1회라 빈도 기준에 못 미쳐 그대로 뒀고, 관형절 중첩 1건은 어절 재배치가 필요해 보존 판단으로 남겼다. 변경률 1.1% 는 게이트의 경고선 30% 와 폐기선 50% 에서 멀다.

### 자리별 품질 근거

| 자리 | 근거 | 어디에 |
| --- | --- | --- |
| 상시 규칙 | 주입 유무 블라인드 24쌍에서 21승 3무 0패 | C5 |
| `korean-writing` 스킬 | im-not-ai 윤문본과 블라인드 56쌍에서 49승 0패 | C9 |
| 윤문 | 이 저장소가 만든 것이 아니라 im-not-ai 의 실측에 기댄다. 여기서 보는 것은 변경률 게이트와 자체검증 통과 여부다 | 위 표 |
| 검사 훅 | 위반 10/10, 정상 오탐 0/5, 실문서 2023년 이전 32개 중 1개 | A1·A2·아래 |

**2026-09-11 실문서 재측정.** `tools/measure.sh ~/Documents` 로 210개를 검사해 84개가 걸렸다(K1 74, K9 28, K10 17, K8 10, K4 8, K7 5, K3 4, K5 4, K2 1). 9.7초 걸렸다. 파일 수정 연도로 가르면 2023년 이전 32개 중 1개, 2024~2025년 1개 중 0개, 2026년 177개 중 83개다. 걸린 84개 가운데 20개가 이 저장소 문서인데 전부 im-not-ai 참조 문서이거나 실험이 남긴 옛 규칙 파일이고, 이 저장소의 산문 문서는 하나도 걸리지 않았다. 코퍼스가 143개에서 210개로 늘어난 것은 im-not-ai 내장으로 저장소 자체의 `.md` 가 늘었기 때문이다.

## H. 주입이 일을 방해하는가 (2026-09-11 신설)

A 부터 G 까지는 전부 **글이 좋아졌는가**를 본다. 이 절은 반대쪽이다. 규칙을 컨텍스트에 넣어서
모델이 일을 덜 잘하게 되지 않았는지 본다. 문체는 채점하지 않는다.

| #   | 기준                                                        | 임계                          |
| --- | ----------------------------------------------------------- | ----------------------------- |
| H1  | 주입이 기술 문제의 정답률을 떨어뜨리지 않는가               | **차이 구간이 0 을 품는다**   |
| H2  | 규칙과 반대되는 것을 사용자가 명시로 요청하면 사용자를 따르는가 | **전건**                      |
| H3  | 서브에이전트 주입이 조사 결과나 답변 언어를 흐리지 않는가   | **정답률 동일, 한글 누출 없음** |
| H4  | 코드 작업에서 글 작성 스킬이 잘못 뜨지 않는가               | **0건, 양성 대조는 1건 이상** |
| H5  | 주입이 설계 답변에서 짚어야 할 개념을 떨어뜨리지 않는가     | **차이 구간이 0 을 품는다**   |
| H6  | 주입이 코드의 경계 조건 처리를 떨어뜨리지 않는가            | **차이 구간이 0 을 품는다**   |
| H7  | 주입이 코드 리뷰의 결함 검출을 떨어뜨리지 않는가            | **차이 구간이 0 을 품는다**   |
| H8  | 주입이 요약에서 원문의 사실을 떨어뜨리지 않는가             | **차이 구간이 0 을 품는다**   |
| H9  | 정보가 없을 때 없다고 말하는 빈도가 줄지 않는가             | **차이 구간이 0 을 품는다**   |
| H10 | 주입이 디버깅의 원인 지목을 떨어뜨리지 않는가               | **차이 구간이 0 을 품는다**   |
| H11 | 서식 규칙이 산문 밖으로 새지 않는가                         | **설계 답변 길이가 주입 없는 쪽과 같다** |
| H13 | 주입이 답변의 세부를 떨어뜨리지 않는가                      | **세부 항목이 주입 없는 쪽과 갈리지 않고 기본값 같은 괄호 세부가 0/8 이 아니다** |

측정은 [`docs/experiments/task-performance/`](experiments/task-performance/README.md) 에 있다.
`run_all.sh` 하나로 넷을 다 돌린다. LLM 을 부르므로 CI 에 넣지 않는다.

**H1 정확도 (2026-09-11).** 답이 하나로 떨어지는 기술 문제 10개를 주입 유무로 갈라 돌렸다.
큰 모델은 대부분 만점이라 저하가 있어도 안 보이므로 여유가 있는 작은 모델을 같이 뒀다.

| 모델 | A 주입 없음 | B 주입 있음 | 차이 95% 구간 |
| --- | --- | --- | --- |
| Haiku 4.5 | 40/40 | 39/40 | −11.5 ~ +7.9%p |
| Opus 5 | 30/30 | 29/30 | −15.0 ~ +10.0%p |

틀린 두 건은 다른 문항이고 같은 문항의 다른 표본은 맞혔다. **구간이 넓다는 것을 그대로 적는다.**
두 조건이 거의 만점이라 이 표본이 배제하는 것은 10%p 를 넘는 저하뿐이다. 그보다 작은 저하가
없다고는 말할 수 없다. 표본을 늘리기보다 여유가 있는 과제를 찾는 쪽이 먼저다.

**H2 지시 준수 (2026-09-11).** 주입문은 이모지를 쓰지 말라고, `첫째·둘째` 병렬을 쓰지 말라고 적는다.
사용자가 바로 그것을 달라고 하면 누구를 따르는지 봤다. 주입 있음 25/25 다. 이모지를 넣어 달라고 하면
넣고, 번호를 붙여 달라고 하면 붙이고, 영어로 답하라고 하면 영어로 답한다. 주입을 끈 쪽이 오히려
이모지 요청 다섯 중 둘을 빠뜨렸다(23/25). **규칙이 사용자를 덮지 않는다.**

**H3 서브에이전트 (2026-09-11).** 저장소에서 값을 찾아오게 하는 과제 둘로 주입 유무를 갈랐다.
양쪽 6/6 이다. 영어로 답하라고 한 과제의 한글 비중은 양쪽 다 2% 이하다. 조건당 표본이 셋이라
구간이 매우 넓다(−39 ~ +39%p). 큰 저하가 없다는 것 말고는 말할 수 있는 것이 없다.

**H4 스킬 오발동 (2026-09-11).** 주입문 마지막 줄이 "글 작성 요청은 korean-writing 스킬을 먼저
로드한다" 다. 코드 작업이 글 작성으로 읽히면 본문 약 10,100 토큰이 헛되이 든다. 코드 작업 프롬프트
넷을 주입 유무로 갈라 여덟 번씩 돌려 스트림에서 `Skill` 호출을 셌다. 양쪽 다 0건이다.
**양성 대조를 같이 뒀다.** 진짜 글 작성 요청 하나에서는 `korean-writing:korean-writing` 이 1건 떴다.
검출기가 도는 것을 확인했으므로 위의 0건이 근거가 된다.

**H5 설계 능력 (2026-09-11). 여기서 저하가 실제로 나왔고, 고쳤다.** 설계 문제 다섯(웹훅 멱등,
멀티테넌트 격리, 대용량 업로드, 캐시, 무중단 마이그레이션)에 조건당 표본 넷씩 돌렸다. 답을 사람이
읽고 견주면 문체에 끌리므로 **빠지면 설계가 틀리는 개념이 실제로 나왔는지**만 셌다. 표현이 어떻든
개념을 짚었으면 맞은 것이다.

| 조건 | 개념 적중 | 답변 길이 중앙값 | A 와의 차이 95% CI |
| --- | --- | --- | --- |
| A 주입 없음 | 151/160 (94%) | 4,884자 | 기준 |
| B 옛 주입문 | 140/160 (88%) | 3,906자 | **[−13.2, −0.3]%p** |
| B 새 주입문 | 143/160 (89%) | 4,531자 | [−11.1, +1.3]%p |

옛 주입문에서는 구간이 0 을 벗어났다. 설계 답변이 20% 짧아지고 짚어야 할 항목이 빠졌다.
원인은 주입문이 불릿과 병렬을 막고 짧게 끝내라고 하는 데 있다. 설계 답변은 실패 모드를 늘어놓아야
하는 글인데 규칙을 지키느라 항목이 떨어져 나갔다. `SKILL.md` 에는 이 방어가 있었다
(`규칙은 표현을 빼라는 것이지 문장을 자르라는 것이 아니다`, `요청받은 분량을 지킨다`).
주입문에만 없었다.

한 줄을 더했다. `규칙은 표현을 고르는 기준이다. 짚어야 할 내용을 빼거나 분량을 줄이는 데 쓰지 않는다.`
길이가 3,906 에서 4,531 로 돌아왔고 구간이 0 을 품게 됐다. **다 해결됐다고는 하지 않는다.**
점 추정은 −6.1%p 에서 −5.0%p 로 조금 움직였을 뿐이고, 구간이 0 을 품는 것과 차이가 없다는 것은 다르다.

그 한 줄로 주입문이 한글 402자에서 437자, 726 토큰에서 784 토큰이 됐다. D2 예산을 1,500 에서
1,600 으로 올렸다. 문체 회귀도 봤다. `regress.sh` 로 옛 규칙과 새 규칙을 블라인드로 붙여
**새 규칙 2승 0패 2무**다. 판정 모델은 `claude-fable-5-1` 의 사용량이 떨어져 `claude-sonnet-5` 로
바꿔 돌렸으므로 지난 판정과 곧바로 견주지 않는다. 그 사고 때문에 `regress.sh` 에
`KW_REGRESS_MODEL` 을 넣었다.

**H6 코드 품질 (2026-09-11).** 모델이 쓴 함수를 실제로 돌려 숨은 테스트에 넣었다. 읽고 판단하지
않는다. 경계 조건을 챙겼는지가 갈리는 지점이라 빈 입력, 맞닿는 구간, 안정 정렬, 멀티바이트 글자,
루트 위로 올라가는 `..` 을 넣었다.

| 모델 | A 주입 없음 | B 주입 있음 | 차이 95% CI |
| --- | --- | --- | --- |
| Opus 5 | 120/120 | 120/120 | [−3.1, +3.1]%p |
| Haiku 4.5 | 160/160 | 160/160 | [−2.3, +2.3]%p |

**양쪽 다 만점이라 이 과제로는 가리지 못한다.** 차이가 없다고 읽으면 안 된다. 과제가 쉬워서
안 보이는 것이다. 표본 단위로 보면 구간이 ±16 에서 ±20%p 로 벌어진다. 더 어려운 과제가 필요하다.

**비용.** 총입력 중앙값이 3,655 에서 4,381 로 726 늘었다(옛 주입문 기준). 출력 길이와
걸린 시간은 갈리지 않았다(출력 384 대 422, 시간 6.5초 대 7.4초).

**H7~H10 나머지 네 영역 (2026-09-11).** H5 에서 나온 저하의 기제는 「규칙이 분량을 줄이면 내용이
빠진다」였다. 같은 기제가 걸릴 만한 자리를 넷 더 골라 쟀다. 전부 **H5 를 고친 뒤의 규칙**으로 잰 값이다.

| 영역 | 무엇을 세나 | 모델 | A 주입 없음 | B 주입 있음 | 차이 95% CI |
| --- | --- | --- | --- | --- | --- |
| H7 코드 리뷰 | 심어 둔 결함 5개 중 몇 개 | Opus | 60/60 | 60/60 | [−6.0, +6.0]%p |
| | | Haiku | 138/160 | 136/160 | [−9.0, +6.5]%p |
| H8 정보 보존 | 원문의 사실 12개 중 몇 개 | Opus | 108/108 | 108/108 | [−3.4, +3.4]%p |
| | | Haiku | 108/108 | 106/108 | [−5.5, +2.8]%p |
| H9 과잉 단정 | 정보 부족을 밝히고 볼 것을 짚는가 | Opus | 18/24 | 19/24 | [−18.9, +27.7]%p |
| | | Haiku | 10/24 | 15/24 | [−2.9, +48.6]%p |
| H10 디버깅 | 원인이 되는 줄을 짚는가 | Opus | 12/12 | 12/12 | [−24.3, +24.3]%p |
| | | Haiku | 12/12 | 11/12 | [−33.5, +18.7]%p |

넷 다 구간이 0 을 품는다. 다만 셋이 큰 모델에서 만점이라 가리는 힘이 약하다.

**짧아지지만 내용은 남는다.** 주입을 받은 쪽이 대체로 짧다. 리뷰 1,172자 대 928자, 요약 1,320자 대
1,030자다. 그런데 결함 검출과 사실 보존은 갈리지 않았다. H5 의 설계와 다른 점이 여기다. 설계는
열거할 항목이 많아 짧아지면 빠졌고, 리뷰와 요약은 항목이 정해져 있어 짧아져도 남는다.

**과잉 단정은 주입 쪽이 조금 낫다.** Haiku 에서 42% 대 62% 다. 구간이 넓어 단정하지 않지만
적어도 규칙이 정직함을 깎는다는 증거는 없다.

**리뷰는 표본을 늘려서 가렸다.** 표본 셋일 때 Haiku 에서 90% 대 83% 로 −7%p 가 나왔다. 여덟로
늘리니 86% 대 85% 로 −1.3%p 가 됐다. 표본 셋짜리 차이를 결과로 냈으면 없는 저하를 보고할 뻔했다.

**측정 도구를 두 번 고쳤다.** 정보 보존과 과잉 단정의 첫 판이 통째로 무효였다. 도구를 없앤 세션에서
모델이 도구 호출 텍스트를 내고 끝났고, 실행 위치가 이 저장소라 저장소를 뒤지려 들었다. 채점기가
그것을 0 점으로 세서 「정보 보존 89% 대 20%」라는 가짜 저하가 나왔다. 프롬프트에 도구가 없다는 것을
적고, 빈 디렉터리에서 돌리고, 채점기가 오염된 표본을 세지 않고 이름을 찍어 알리도록 고쳤다.

**H11 서식 규칙의 적용 범위 (2026-09-11).** H5 를 고치고도 설계의 점 추정이 −5.0%p 로 남았다.
주입문을 다시 보니 서식 금지가 조건 없이 전부에 걸리고 있었다. 12행이 코드·로그·명령어를 빼 두지만
7행의 `줄표 금지, 첫째·둘째 병렬 금지, 불릿은 목록일 때만` 은 기술 답변에도 그대로 적용된다.
설계 답변은 실패 모드를 늘어놓아야 하는 글이라 그 자리에서 형태를 잃었다.

7행을 고쳐 서식 규칙의 적용 범위를 **사용자에게 내는 산문**으로 좁혔다. 목록·절차·비교·표처럼
형태가 곧 내용인 답은 그 형태를 지키라고 적었다.

| 판 | 설계 개념 적중 | 설계 답변 길이 | 차이 95% CI |
| --- | --- | --- | --- |
| A 주입 없음 | 151/160 (94%) | 4,884자 | 기준 |
| v1 옛 주입문 | 140/160 (88%) | 3,906자 | [−13.2, −0.3]%p |
| v2 분량 방어 한 줄 (H5) | 143/160 (89%) | 4,531자 | [−11.1, +1.3]%p |
| v3 서식 범위 축소 (H11) | **148/160 (92%)** | **4,909자** | [−7.6, +3.9]%p |

길이가 주입 없는 쪽과 같아졌다(4,884 대 4,909). 서식 금지가 분량을 누르던 것이 원인이었다는 뜻이다.

**차이가 없던 영역은 다시 재지 않았다.** 두 조건이 만점이던 코드 품질·정보 보존·디버깅은 그대로
두고, 여유가 있던 설계·코드 리뷰·과잉 단정·지시 준수만 다시 돌렸다.

| 영역 | v2 | v3 | 읽는 법 |
| --- | --- | --- | --- |
| 설계 (Opus) | 89% | **92%** | 올랐다 |
| 코드 리뷰 (Haiku) | 85% | 82% | 표본 흔들림. 160개 중 4개 차이다 |
| 과잉 단정 (Opus) | 79% | 75% | 표본 24 |
| 과잉 단정 (Haiku) | 62% | 50% | 표본 24. 둘 다 주입 없는 쪽 42% 보다 높다 |
| 지시 준수 (Opus) | 25/25 | 25/25 | 그대로. 길이는 740 에서 898 로 돌아왔다 |

주입문이 한글 437자에서 463자, 784 토큰에서 816 토큰이 됐다. 상한을 480 으로 올렸고 상시 비용은
1,540 으로 D2 예산 1,600 안이다. 문체 게이트는 v1 대 v3 로 **1승 1패 2무**다. 회귀는 없고
문체가 좋아졌다는 증거도 없다.

**이 절이 못 보는 것.** 과제가 전부 한 턴에서 끝난다. 스무 턴짜리 코딩 작업에서 규칙이 판단을
흐리는지는 재지 않았다. 정확도 시험은 도구를 끄고 돌리므로 도구를 고르는 판단에 주는 영향도 별개다.
코드 과제는 두 모델 다 만점이라 가리는 힘이 없다(H6). 설계 쪽은 개념이 나왔는지만 세므로
그 개념을 **옳게** 썼는지는 보지 않는다. H7~H10 도 셋이 큰 모델에서 만점이라 같은 한계가 있다.
H9 의 정직함은 표현이 있었는지로 세므로 그 표현이 진짜 판단이었는지는 가리지 못한다.

**H13 세부 보존과 주입 제거 (2026-09-11). 여기서 떨어졌고 주입을 뺐다.** 사용자가 README 의 전후 예시를 보고 주입한 답에서 세부가 빠진다고 짚었다. 그 예시는 H5·H11 을 고치기 전 규칙(`b782708`)으로 만든 것이었고 README 의 「사실 내용은 두 답이 같습니다」 도 틀린 문장이었다. 그래서 같은 질문을 지금 규칙으로 다시 쟀다.

디바운스 훅을 설명하고 cleanup 이 왜 필요한지 묻는 질문(`docs/experiments/always-on/prompts/01.txt`)을 claude-sonnet-5 에 조건마다 8번 물었다. 답에 나올 만한 세부 11개를 정하고 어느 조건인지 모르는 claude-opus-5 에게 항목마다 설명됐는지 true/false 로 판정하게 했다. 처음에는 정규식으로 셌는데 「중간 글자들」·「전부 살아남아」 같은 표현을 놓쳐 5/10 을 받은 답이 실제로는 8점 가까웠다. 정규식 결과는 버렸다.

| 조건 | 세부 11항목 | `delay` 기본값 300ms | 주입 없음과의 차이 |
| --- | --- | --- | --- |
| A 주입 없음 | 72/88 (82%) | 5/8 | 기준 |
| B 지금 규칙 | 64/88 (73%) | **0/8** | 순열검정 p=0.17, 기본값 Fisher p=0.026 |
| S 줄인 규칙 | 62/88 (70%) | **0/8** | 순열검정 p=0.097, 기본값 Fisher p=0.026 |
| N 같은 길이의 중립 메모 | 67/88 (76%) | 4/8 | 순열검정 p=0.36, 기본값 Fisher p=1.0 |

S 는 문장을 다시 짜게 하는 규칙(대구·병렬·불릿·문장 길이·「사람이 말하듯」)을 빼고 어휘를 바꾸는 규칙만 남긴 판이다. 「괄호 보충, 긴 문장, 목록·표·소제목은 필요하면 그대로 쓴다. 기본값·조건·예외 같은 세부를 빼지 않는다」 를 적어 넣었는데도 기본값이 한 번도 나오지 않았다. N 은 문체와 관계없는 작업 환경 메모를 S 와 같은 길이로 넣은 대조군이다. 캐시에 쓰인 입력 토큰은 A 5,841 대비 B +832, S +643, N +647 로 셋 다 실제로 들어갔다.

**빠진 것은 곁가지 세부다.** cleanup 호출 시점, 타이머가 쌓임, 옛 값이 반영됨, 마지막 값만 남음 같은 뼈대는 A 40/40, B 39/40 으로 거의 같았다. 갈린 것은 기본값과 의존성처럼 곁들여 적는 세부였고 A 에서 기본값은 다섯 번 모두 `delay`(기본 300ms) 처럼 괄호 안에 있었다. 산문 속 괄호 보충이 A 중앙값 4개에서 B 1개로 줄었다. 규칙집의 대조 코퍼스에서도 사람 글이 AI 글보다 괄호를 많이 쓴다(1,000문장당 10.6 대 1.2, `empirical-validation.md`). 괄호를 줄이면 사람 글에서도 멀어진다.

**뺐다.** 시험 전에 합격 기준 셋을 정했다. 디바운스 세부에서 A 와 갈리지 않고 기본값이 0/8 이 아닐 것, 설계 개념(H5 세트)의 차이 구간이 0 을 품고 길이가 85% 이상일 것, 문체 블라인드에서 주입 없는 쪽에 지지 않을 것이다. 하나라도 떨어지면 주입을 빼기로 했고 S 가 첫 기준에서 떨어졌다. 나머지 둘은 결정을 바꿀 수 없어 도중에 멈췄으므로 결과가 없다. 같은 문제로 규칙을 고친 것이 이미 두 번(H5, H11)이었고 세 번째 판도 막지 못했다.

뺀 것은 `SessionStart` 훅과 `SubagentStart` 훅, `always-on.md`, 플러그인 설정 `always_on`·`subagent_rules`, 두 훅의 회귀 테스트다. 상시 비용은 약 1,540 에서 약 730 토큰이 됐다(D2). C5 의 블라인드 21승은 문체만 본 값이라 이 결과와 부딪히지 않는다. 문체는 나아졌고 그 대가로 세부가 빠졌다.

**이 측정이 못 보는 것.** 질문 하나에 모델 하나다. 세부 항목은 판정자의 true/false 라서 설명이 얼마나 정확했는지는 보지 않는다. 규칙의 어느 줄이 괄호를 줄였는지는 줄마다 빼 보지 않아 모른다. 재현 스크립트는 [`docs/experiments/detail-retention/`](docs/experiments/detail-retention/README.md) 에 있다.

## I. 검사 훅의 안내가 고치면서 정보를 지우게 하지 않는가 (2026-09-11 신설)

모델은 훅이 알린 대로 고친다. 그러니 안내 문구가 곧 교정 지시다. 안내가 규칙집의 처방을 한 줄로 줄이면서 보존 조건을 떨어뜨리면 고치는 동안 정보가 지워진다.

| #   | 기준                                                     | 임계                            |
| --- | -------------------------------------------------------- | ------------------------------- |
| I1  | 훅 알림을 받고 고친 글에서 원문의 정보가 사라지지 않는가 | **대조군과 실제 초안 모두 0건** |

**I1 (2026-09-11).** 모델에게 초안을 `doc.md` 로 저장하게 하고 PostToolUse 검사 훅만 켰다. 훅이 걸리면 모델이 알림을 받고 고친다. 처음 저장한 글과 고친 글을 조건을 모르는 claude-opus-5 에게 주고 사라진 정보를 찾게 했다. 초안은 두 종류다. 하나는 K1·K4·K5·K9·K10 이 한꺼번에 걸리도록 쓴 배포 공지 대조군이고 Sonnet 5 와 Opus 5 로 세 번씩 돌렸다. 다른 하나는 글 작성 프롬프트 14개를 Sonnet 5 와 Haiku 4.5 에 두 번씩 쓰게 해서 훅에 걸린 실제 초안 15개다. 같은 초안을 같은 모델에 주고 안내만 바꿨다.

| 초안                     | 옛 안내     | 새 안내   |
| ------------------------ | ----------- | --------- |
| 대조군 6건               | 9건 사라짐  | **0건**   |
| 훅에 걸린 실제 초안 15건 | 4건 사라짐  | **0건**   |

대조군에서 사라진 9건은 전부 부정한 쪽이었다. 「도구는 바뀌지 않는다」 와 「보안 패치 예외는 절차 우회가 아니다」 같은 대비다. K9 안내가 `A가 아니라 B다 → B다` 였으니 지시대로 한 것이다. 실제 초안의 4건 가운데 둘도 대비(`정책의 대상이 아니라 주체`, `돈을 더 쓰는 것이 아니라`)였고 하나는 결론 문장의 대상이었다. 나머지 하나는 「의미가 크다」 라는 평가인데 규칙집 D-2 가 지우라고 하는 표현이라 실제 손실로 보기 어렵다. 옛 안내로 처음 쟀을 때는 K4 와 K5 를 고치면서 걸린 표현 밖의 문장까지 다시 써서 「세 가지」 같은 개수와 「현대 경제에서」 같은 한정어가 빠지고 「가르치려 한다」 가 「가르친다」 로 단정이 된 것도 봤다(Haiku 가 자기 초안을 고친 실행).

**안내를 규칙집 처방으로 되돌렸다.** 규칙집(`plugin/skills/humanize-korean/references/rewriting-playbook.md`)은 이 셋을 이렇게 처방한다. C-8 대구는 한쪽을 길게 풀고 다른 쪽을 짧게 끊거나 한 문장을 둘로 나누고 효과적인 대구 한두 개는 남긴다. D-1 의 `~라고 할 수 있다` 는 원문이 이미 확정한 경우에만 `~이다` 로 줄이고 유보를 단정으로 올리지 않는다. C-1 병렬은 열거가 핵심이면 표지만 바꾸고 장식일 때만 산문으로 녹인다. 훅 안내는 이 조건을 빠뜨리고 있었다. K9 은 둘로 나누는 방법으로 고쳤다. 규칙집의 다른 예시(`속도가 아니라 방향` → `속도보다 방향이 중요하다`)는 부정을 비교로 바꿔 주장의 강도가 달라진다. 옛 안내 대조군에서 Opus 가 「도구보다 일하는 방식」 으로 바꿔 도구도 바뀔 수 있다고 읽히게 된 것이 그 경우다. 모든 알림 끝에 「걸린 표현만 고친다. 수치·개수·조건·유보 표현과 걸리지 않은 문장은 그대로 둔다」 를 붙였다. 규칙집 대원칙의 국소성과 의미 불변이다. `tests/test_posttooluse.py` 가 이 네 안내를 회귀로 본다.

**한계.** 초안을 사용자가 준 글로 건넸다. 실제로는 모델이 자기 글을 고친다. 15건 중 고친 것은 옛 안내 9건, 새 안내 11건이고 고치지 않은 것은 전부 Haiku 였다. 스킬을 끈 채 훅만 켰으므로 알림이 가리키는 `korean-writing` 스킬을 불러 고치는 경우는 재지 않았다. 사라진 정보는 판정자가 세고 그중 넷을 사람이 전후를 대조해 확인했다.

**I1 재측정 (2026-09-11, 위치를 붙인 알림).** 알림에 걸린 자리의 줄 번호와 발췌를 붙인 뒤 대조군으로 다시 쟀다. 기준은 측정 전에 정했다. 옛 훅(v1.3.0)과 새 훅을 같은 조건(Sonnet 5 와 Opus 5 각 6회)으로 돌려 정보가 사라진 표본이 새 훅에서 더 많고 Fisher 단측 p<0.10 이면 위치 줄이 해를 끼친 것으로 보고 고친다.

| 훅                  | 표본 | 고친 표본 | 정보가 사라진 표본 | 약해짐 합계 |
| ------------------- | ---- | --------- | ------------------ | ----------- |
| 옛 훅 (v1.3.0)      | 12   | 12        | 0                  | 28          |
| 새 훅, 같은 때      | 12   | 11        | 0                  | 26          |
| 새 훅, 먼저 돌린 것 | 6    | 6         | 1                  | 11          |

새 훅을 합치면 고친 17건 중 1건, 옛 훅은 12건 중 0건이라 기준에 들지 않는다(Fisher 단측 p=0.586). 위치 줄은 그대로 둔다. 사라진 1건은 「일하는 방식이 바뀌는 것이니 궁금한 점은 채널에 남겨 달라」 의 인과가 K9 처방대로 문장을 둘로 나누며 끊긴 것이다. K9 안내는 옛 훅과 같은 문구라 이 손실은 처방 자체에서 나올 수 있다. 그래도 이 표본은 I1 의 임계 「0건」 을 깼다. 나눌 때 이유를 잇는 말을 남기라고 안내에 적을지는 표본을 크게 해서 다시 봐야 해서 이번에는 바꾸지 않았다. 새 훅의 Opus 표본 하나는 알림을 받고도 고치지 않았다. 사용자가 준 초안이라 그대로 저장하고 지적 사실만 알렸다.

## J. 쓸 때 스킬을 먼저 묻는가 (2026-09-11 신설)

| #   | 기준                                                                                  | 임계                                          |
| --- | ------------------------------------------------------------------------------------- | --------------------------------------------- |
| J1  | 모델이 korean-writing 스킬을 스스로 부를 때 사용자 확인 없이 스킬이 적용되지 않는가    | **헤드리스 기본·bypass 모드 모두 거부**       |
| J2  | 사용자가 직접 치는 `/korean-writing` 과 다른 스킬·도구는 막지 않는가                  | **회귀 테스트 전건, 직접 호출은 훅에 걸리지 않음** |
| J3  | 스킬로 쓴 글에서 적어 준 사실과 덧붙이는 설명이 빠지는가 (확인 창 문구의 근거)        | **측정 전에 정한 기준으로 과제별 판정**        |
| J4  | 적용 여부를 한국어로 묻고 거절해도 작업이 멈추지 않는가                               | **헤드리스에서 막힌 뒤 규칙 없이 끝까지 씀. 회귀 테스트 전건** |

**J1·J2 (2026-09-11).** H13 에서 문체 규칙이 답변의 세부를 빼는 것을 봤다. `korean-writing` 스킬은 같은 규칙집을 쓰고 이 스킬이 세부를 빼는지는 아직 재지 않았다. 그래서 모델이 스스로 이 스킬을 부를 때 사용자가 먼저 정하게 했다. PreToolUse 훅(`plugin/hooks-handlers/pretooluse-skill.sh`)이 `Skill` 호출을 받아 `korean-writing` 이면 `permissionDecision: "ask"` 를 돌려준다. 2026-09-11 에 권한 창 대신 Claude 가 한국어로 묻는 방식으로 바꿨다(J4).

격리 세션(`--plugin-dir plugin`, `--setting-sources ""`)에서 claude-sonnet-5 로 쟀다. 날짜와 사유까지 준 평범한 슬랙 공지 요청에서 모델이 `korean-writing:korean-writing` 을 스스로 불렀고 훅이 그 호출을 받았다. 헤드리스에서는 확인 창을 띄울 수 없어 기본 모드와 `bypassPermissions` 모드 둘 다 `permission_denials` 에 들어갔다. 기본 모드에서는 모델이 적용할지 되물었고 bypass 모드에서는 규칙 없이 공지를 썼다. 사용자가 `/korean-writing:korean-writing` 을 직접 친 실행에서는 훅이 받은 호출이 0건이었다. `tests/test_pretooluse.py` 22건이 두 이름(`korean-writing`, `korean-writing:korean-writing`)에만 묻고 다른 스킬 셋과 다른 도구, 끄기 스위치, 이상 입력은 출력 없이 exit 0 으로 지나가는 것을 본다.

묻는 문장은 한국어로 쓰고 모델이 넘긴 요청 요약(`args`)을 60자까지 「」 안에 보여 준다. 무엇을 쓰려는지 알아야 거절할지 정할 수 있어서다. 측정 절 번호 같은 근거는 문장에 넣지 않고 주의 문구는 아래 J3 에서 잰 만큼만 말한다.

**못 본 것.** 대화형 세션에서 확인 창이 뜨는 모습은 보지 못했다. 공식 문서에는 `ask` 의 사유가 확인 창에 보이는지 적혀 있지 않아 같은 문장을 `systemMessage` 로도 낸다. 공식 문서는 `ask` 가 평소 권한 흐름을 따른다고 적는다. 허용 목록에 이 스킬이 들어 있으면 묻지 않고 넘어갈 수 있다. 스킬 자체의 측정은 아래 J3 에 있다.

**J3 스킬이 세부를 빼는가 (2026-09-11).** 확인 창의 주의 문구를 무엇으로 쓸지 정하려고 스킬 자체를 쟀다. 기준은 측정 전에 정했다. 과제별 표본 합계 순열검정 p<0.05 로 스킬 쪽이 낮거나 항목 하나라도 Fisher p<0.05 로 줄면 「빠진다」, 둘 다 아니면 「증거 없음」이다.

`plugin/SKILL.md` 를 시스템 프롬프트에 붙인 조건(S)과 붙이지 않은 조건(A)을 claude-sonnet-5 로 과제마다 8번씩 돌리고 조건을 모르는 claude-opus-5 가 항목마다 판정했다. 두 조건 모두 도구 없이 본문만 내라는 같은 안내를 받았다.

| 과제                                      | A       | S               | 한글 길이 중앙값 | 판정 |
| ----------------------------------------- | ------- | --------------- | ---------------- | ---- |
| 사실을 준 슬랙 공지 (12항목)              | 96/96   | 96/96           | 203 → 170        | 증거 없음 |
| 사실을 준 장애 보고서 (15항목)            | 120/120 | 120/120         | 292 → 262        | 증거 없음 |
| 사실을 주지 않은 설명 문서 (디바운스, 11항목) | 72/88   | 63/88 (p=0.147) | 661 → 450        | 빠진다. `delay` 가 바뀌어도 타이머를 새로 건다는 설명 8/8 → 3/8 (Fisher p=0.026) |

원인을 가르려고 같은 글자 수의 무관한 문서(행동 강령·기여 안내·글자 수 설명서·보안 정책을 이어 붙인 것)를 붙인 대조군(N)을 설명 문서 과제에 돌렸다. N 은 70/88(A 대비 p=0.84), 같은 항목 6/8(p=0.47), 길이 610자로 A 와 갈리지 않았다. 설명이 짧아지고 곁가지가 빠진 원인은 규칙이다. 빠졌다고 판정된 항목은 S 원문 8개를 사람이 읽어 판정이 맞는 것을 확인했다.

그래서 확인 창의 주의 문구는 잰 만큼만 말한다. 「요청에 적은 내용은 그대로 담지만 Claude 가 덧붙이는 설명은 짧아질 수 있습니다」. 처음 쓴 「세부가 빠질 수 있으니 날짜·수치·조건이 중요한 문서라면 거절하세요」 는 날짜와 수치가 빠진다고 읽히는데 실측에서 그것들은 하나도 빠지지 않았다.

**한계.** 설명 과제는 하나, 모델은 하나, 표본은 조건마다 8이다. 중립 문서는 글자 수를 맞췄지만 영어가 섞여 실제 주입 토큰이 약 7,700 으로 스킬(약 10,100)보다 적다. 스킬을 Skill 도구로 불러오는 실제 경로 대신 시스템 프롬프트로 붙였다. 재현 스크립트는 `docs/experiments/detail-retention/skill/` 에 있다.

**J3 재측정 (2026-09-11, 문체 따르기 절을 넣은 판).** 스킬에 「쓰는 사람의 문체를 따른다」 절을 넣고 README 스킬을 가리키던 한 줄을 뺀 뒤 S 조건만 새로 돌렸다. A 와 N 은 스킬과 무관해 앞의 생성을 그대로 썼다. 기준과 모델, 표본 수는 위와 같다.

| 과제                  | A       | S (앞 판)       | S (이번 판)     | 한글 길이 중앙값 A → S(이번) | 판정 |
| --------------------- | ------- | --------------- | --------------- | ---------------------------- | ---- |
| 슬랙 공지 (12항목)    | 96/96   | 96/96           | 96/96           | 203 → 164                    | 증거 없음 |
| 장애 보고서 (15항목)  | 120/120 | 120/120         | 120/120         | 292 → 246                    | 증거 없음 |
| 설명 문서 (11항목)    | 72/88   | 63/88 (p=0.147) | 62/88 (p=0.057) | 661 → 461                    | 증거 없음. `delay` 설명 8/8 → 4/8 (Fisher p=0.077) |

설명 문서가 이번에 기준을 넘지 않았다고 좋아진 것은 아니다. 합계는 앞 판과 거의 같고 가장 크게 준 항목도 같다. 이번에는 어느 항목도 Fisher p<0.05 에 들지 않았을 뿐이다. 그래서 확인 문구가 말하는 범위는 그대로 둔다. 새 절은 사용자가 표본을 줄 때 도는 규칙인데 이 과제들에는 표본이 없어 그 효과는 재지 않았다.

**J3 후속: 「설명은 넉넉히」 선택지가 정말 설명을 되살리는가 (2026-09-13).** 확인 창에 셋째 선택지를 넣기로 하고, 고른 대로 글이 달라지는지 먼저 쟀다. 조건 M 은 스킬 본문에 「규칙을 지키면서 요청에 없던 곁가지 설명을 평소만큼 덧붙여 쓴다. 설명을 줄이지 않는다」를 더한 것이고, 훅이 그 선택지에 지시하는 말과 같은 문구를 쓴다. A 와 S 는 앞의 생성을 그대로 쓰고 M 만 새로 만들었다(과제 셋 × 표본 여덟, claude-sonnet-5). 판단 기준은 측정 전에 정했다. M 이 S 보다 유의하게 높고 A 수준에 가까우면 선택지를 남기고, S 와 구별되지 않으면 뺀다.

| 과제                  | A       | S       | M           | 한글 중앙값 A → S → M | M 대 S 순열검정 |
| --------------------- | ------- | ------- | ----------- | --------------------- | --------------- |
| 슬랙 공지 (12항목)    | 96/96   | 96/96   | 96/96       | 203 → 164 → 175       | p=1.000 (셋 다 만점) |
| 장애 보고서 (15항목)  | 120/120 | 120/120 | 120/120     | 292 → 246 → 283       | p=1.000 (셋 다 만점) |
| 설명 문서 (11항목)    | 72/88   | 62/88   | **72/88**   | 661 → 461 → 564       | **p=0.014**     |

설명 문서에서 M 이 A 와 같은 72/88 로 돌아왔고 S 보다 유의하게 높다. 길이도 절반쯤 회복했다. 미리 정한 기준을 넘었으므로 선택지를 남긴다. 다만 항목 단위로 되살아났다고 말할 수는 없다. 개별 항목에서 Fisher p<0.05 에 든 것은 없고 유의한 것은 합계뿐이다. 공지와 장애 보고서는 세 조건이 모두 만점이라 이 과제들로는 차이를 볼 수 없다. 요청에 적힌 사실만 담는 짧은 글에는 줄어들 곁가지 설명 자체가 없어서다.

**블라인드 판정 (2026-09-11, 문체 따르기 절을 넣은 판).** `docs/experiments/skill-vs-imnotai/run.sh` 를 기본 네 프롬프트(01·05·L1·L4)로 돌렸다. 생성과 판정 모두 claude-opus-5 이고 쌍마다 순서를 바꿔 두 번 물었다. 스킬 3승, im-not-ai 1승이고 무승부와 순서에 따라 갈린 쌍은 없다. im-not-ai 윤문의 변경률은 01 0.0%, 05 0.2%, L1 18.3%, L4 2.9% 였고 01 은 윤문 전과 한 글자도 다르지 않았다. README 의 전후 비교 그림은 이 판정의 L1 쌍에서 판정자가 짚은 문장을 뽑은 것이다. 그 전에 릴리스 노트 요청으로 규칙 없는 글과 스킬 글을 한 번씩 만들어 붙였는데 한 순서는 무승부, 다른 순서는 스킬이어서 순서에 따라 갈렸다. 차이가 작아 그림에 쓰지 않았다. 이 스크립트가 스킬 없이 돌던 것을 고친 뒤 처음 돈 판정이다.

진 쌍은 05(장애 공지, 400자 요청)다. 판정자는 두 순서 모두 스킬 쪽의 「로그인할 수 없었습니다」·「로그인할 수 있게 되었습니다」 처럼 `~할 수 있다` 가 되풀이된 것을 짚었다. 이 표현은 스킬의 품질 바 표에 이미 있는 항목이고 새 절은 표본이 있을 때만 도는 규칙이라 새 절 때문으로 보지 않는다. 스킬 쪽 한글은 223자로 요청한 400자에 크게 못 미쳤다(C10 의 분량 결함). im-not-ai 쪽은 본문 뒤에 분량 보고와 채운 부분 안내가 붙어 있었는데 판정자는 감점하지 않았다. 56쌍 49승 0패 뒤로 처음 진 쌍이다. 네 쌍뿐이라 승률을 다시 말할 근거는 되지 못하고 `~할 수 있다` 반복과 분량 미달은 다음에 규칙을 고칠 때 볼 항목으로 남긴다.

**J4 권한 창 대신 Claude 가 한국어로 묻는다 (2026-09-11).** 사용자 보고로 바꿨다. 권한 창(`ask`)은 선택지가 영어라 읽기 어려웠고 거절하면 작업이 멈췄다. Claude Code 는 권한 창에서 거절된 도구 호출을 사용자 지시를 기다리는 거부로 처리한다. 그래서 훅이 호출을 막고(`deny`) 그 사유로 Claude 에게 AskUserQuestion 으로 한국어 질문을 띄우게 했다. 질문에는 무엇을 어디에 쓰는 글인지 넣게 하고 선택지는 「적용」·「적용 안 함」 이다. 사용자의 답은 세션 기록(`transcript_path`)에서 이번 요청 뒤에 남은 AskUserQuestion 결과로 찾는다. 기록의 형식은 이 저장소를 고친 세션의 실제 기록에서 확인했다. 결과 항목의 `toolUseResult.answers` 에 질문과 고른 선택지가 들어간다.

헤드리스(claude-sonnet-5, `--plugin-dir plugin`)에서 쟀다. 헤드리스에는 AskUserQuestion 이 없다.

| 실행                          | 스킬 호출   | 결과                   | 글 앞에 붙은 말                                |
| ----------------------------- | ----------- | ---------------------- | ---------------------------------------------- |
| 슬랙 공지, 첫 안내문          | 1회 막힘    | 규칙 없이 끝까지 씀    | 「AskUserQuestion 도구를 쓸 수 없어서…」       |
| 슬랙 공지, 첫 안내문 다시     | 2회 막힘    | 규칙 없이 끝까지 씀    | 같은 말                                        |
| `배포-QA.md` 작성, 첫 안내문  | 1회 막힘    | 규칙 없이 파일을 씀    | 없음                                           |
| 슬랙 공지, 고친 안내문 두 번  | 1회씩 막힘  | 규칙 없이 끝까지 씀    | 한 번 붙음, 한 번 없음                          |

어느 실행도 멈추지 않았다. 안내문을 두 번 고쳐 스킬을 되풀이해 부르는 일은 없앴지만 도구가 없다는 말이 글 앞에 붙는 것은 두 번 중 한 번 남았다. 같은 문제를 두 번 고쳐도 다 풀리지 않아 여기서 멈췄다. 이 말은 질문 도구가 없는 헤드리스에서만 나온다. 스킬 호출은 파일을 쓰기 전에 막혔으므로 `.md` 파일로 쓰는 요청에서도 쓰기 전에 묻는다.

**못 본 것.** 대화형 창에서 질문이 뜨고 「적용」 을 누르는 흐름은 직접 보지 못했다. 그 경로는 실제 기록 형식을 본뜬 회귀 테스트 36건으로 본다. 답이 담긴 기록이 훅이 도는 시점에 이미 파일에 쓰여 있는지도 확인하지 못했다. 쓰여 있지 않으면 훅은 한 번 더 묻는다.


## K. 검사 훅에 위치와 설정을 붙여도 판정이 그대로인가 (2026-09-11 신설)

검사 훅이 걸린 자리의 줄 번호와 발췌를 붙이고, 규칙을 골라 끄는 설정 넷(파일 머리의 `disable` 표시, 저장소의 `.korean-writing.json`, 환경변수 `KOREAN_WRITING_DISABLE_RULES`, 플러그인 설정 `disabled_rules`)을 읽게 됐다. 판정(코드와 횟수)은 한 글자도 바뀌면 안 된다. 그래서 판정은 예전 경로로 계산하고 위치는 같은 정규식을 줄과 열을 보존한 사본에 따로 돌려 찾는다.

| 확인 | 결과 |
| --- | --- |
| 이 기계의 한글이 든 `.md` 304개를 옛 훅(v1.3.0)과 새 훅에 같은 Write 입력으로 넣고 종료 코드와 판정 줄을 비교 | 옛 훅이 건 파일 97개, 판정이 달라진 파일 0개 |
| 새 훅이 낸 위치 줄 | 602개 모두 줄 번호가 붙었다 |
| 회귀 테스트 | 86건 통과. 규칙 골라 끄기 12건, 저장소 설정 7건, 위치 5건을 더했다 |

처음 옮겨 쓴 판은 304개 중 15개에서 판정 줄이 달랐다. K10 안내 문구의 「규칙집을」이 「규칙을」로 바뀌어 있었고 이 비교로 찾아 되돌렸다.

규칙을 끄는 방법은 훅의 알림에 적지 않았다. 알림에 있으면 모델이 표현을 고치는 대신 규칙을 끌 수 있다. 이 위험은 재지 않고 설계로 막았다. 위치를 붙인 알림이 고치는 동안의 세부 보존에 주는 영향은 I 절의 재측정에 적었다.

## L. 블라인드 판정을 믿을 수 있는가 (2026-09-11 신설)

C9 의 56쌍 49승 0패를 두고 판정 근거를 물었다. 짚어 보니 셋이 걸린다.

1. 기준서(`docs/experiments/skill-vs-imnotai/judge-rubric.md`)의 감점 목록이 스킬 규칙과 거의 같다. 판정은 「더 자연스러운가」보다 「이 목록을 더 잘 피했는가」에 가깝다.
2. 판정자가 사람이 아닌 AI 이고 글을 쓴 모델과 같은 claude-opus-5 였다. AI 판정은 먼저 보인 글, 긴 글, 자기와 비슷한 글을 고르는 경향이 알려져 있다(Zheng 외 2023, MT-Bench). 순서를 바꿔 두 번 묻고 두 번 같은 답만 센 것은 첫째 경향에 대한 대책이다.
3. 사람이 판정을 검증한 적이 없다.

im-not-ai 쪽 글에는 파이프라인의 `HUMANIZE-SUMMARY` 블록과 본문 뒤 안내도 남아 있었고 판정자가 이를 감점했다. 실제로 받는 본문에는 들어가지 않는 것이라 im-not-ai 에 불리했다.

**다시 재는 설계 (결과를 보기 전에 정함).**

- 글: 프롬프트 14개(단문 01~08, 장문 L1~L6)를 새로 생성한다. 스킬로 처음부터 쓴 글과 규칙 없이 쓴 뒤 im-not-ai 로 윤문한 글이다. 생성은 claude-opus-5 다.
- 본문: 판정 전에 양쪽 모두 요약 블록을 걷어내고, 마지막 `---` 줄이 글의 뒤쪽 절반에 있으면 그 뒤도 걷어낸다.
- 판정 셋: 주 결과는 중립 기준서(`judge-rubric-neutral.md`) × claude-sonnet-5 다. 중립 기준서는 감점 목록 없이 「한국어를 모국어로 쓰는 사람이 직접 쓴 것처럼 읽히는 쪽」만 묻는다. 같은 기준서 × claude-opus-5 와 옛 기준서 × claude-opus-5 를 함께 돌려 판정 모델과 기준서의 효과를 본다.
- 셈: 순서를 바꿔 두 번 묻고 두 번 같은 답일 때만 승패로 센다. 부호 검정은 승패만 놓고 양측으로 낸다.
- 판단: 주 결과에서 스킬 승이 im-not-ai 승보다 많고 p<0.05 면 「중립 판정에서도 스킬이 낫다」, 반대 방향으로 p<0.05 면 「im-not-ai 가 낫다」, 그 밖에는 「우열을 말할 수 없다」 로 적는다. README 는 결과가 어느 쪽이든 이 판단으로 바꾼다.
- 사람: 같은 14쌍을 평가 페이지로 블라인드 평가받는다. 글 A·B 순서는 쌍마다 섞고 어느 쪽이 스킬인지는 `docs/experiments/skill-vs-imnotai/human/mapping.json` 에만 둔다. 평가자별 결과, 다수 의견, 다수 의견과 각 AI 판정의 일치를 적는다. 평가자가 적으면 결론 없이 수치만 적는다.

**사람 평가에 질문을 하나 더했다 (2026-09-13).** 답을 받기 전에 고쳤고 받은 답은 없다. 쓰는 사람이 물었다. 한쪽이 자연스럽고 다른 쪽이 짜임새가 나을 수 있는데 한 가지만 물으면 그 갈림이 기록되지 않는다. 실제로 판정문에도 「먼저·다음으로·마지막으로로 기계적으로 늘어놓고」처럼 짜임새를 짚은 대목이 있고 훅에도 `K5` 기계적 병렬 규칙이 있는데, 지금까지 짜임새를 따로 센 적은 없었다.

- 주 결과는 그대로 자연스러움이다. AI 판정이 이 기준으로 판단했으므로 일치율을 내려면 바뀌면 안 된다.
- 보조로 「글의 짜임새가 더 나은 쪽」을 묻는다. 목차형 정리와 문단 순서, 늘어놓는 방식이다.
- 내용 유실은 묻지 않는다. 평가자에게 원문과 항목 목록을 주지 않아 판단할 수 없고, J3 가 항목별 참·거짓으로 이미 더 정확히 센다.
- 두 질문의 답이 갈린 쌍의 수를 함께 적는다. 많이 갈리면 판정 기준서를 둘로 나눠야 한다는 근거가 되고, 거의 갈리지 않으면 하나로 묶어도 된다는 근거가 된다.

**재는 도중에 고친 것 (2026-09-13).** 측정 스크립트에 결함이 있었다. 「이 세션에는 도구가 없다」 안내를 윤문 호출에만 붙이고 글을 만드는 호출에는 빠뜨렸다. 그 말을 못 들은 모델이 첫 턴에 도구를 부르려다 턴을 다 써서 본문 대신 `Error: Reached max turns (1)` 28자를 남겼다. L1 과 L3 이 그렇게 죽었고 한 쌍이 죽으면 뒤의 판정 84건까지 돌지 못했다. 같은 프롬프트와 같은 턴 한도에 이 안내만 붙여 따로 다섯 번 부르니 모두 본문이 나왔다. 규칙을 붙인 쪽과 안 붙인 쪽에 똑같은 문구를 넣어 쌍 안의 조건을 맞추고 14쌍을 모두 새로 만들었다. 새 실행에서는 생성 42건이 전부 정상이고 재시도가 한 번도 없었다. 출력은 `out/run-notool-20260913/` 이다. 이 안내는 도구를 쓰지 말라는 말일 뿐 문체 지시는 아니지만, 조건이 바뀐 것은 사실이므로 옛 실행의 숫자와 섞지 않는다.

**결과 (2026-09-13, 14쌍).**

| 판정 | 스킬 승 | im-not-ai 승 | 갈림 | 실패 | 부호 검정 |
| ---- | ------- | ------------ | ---- | ---- | --------- |
| **주 결과** 중립 기준 × claude-sonnet-5 | 6 | 1 | 7 | 0 | p=0.125 |
| 중립 기준 × claude-opus-5 | 12 | 1 | 1 | 0 | p=0.003 |
| 옛 기준서 × claude-opus-5 | 13 | 1 | 0 | 0 | p=0.002 |

「갈림」은 순서를 바꾸자 답이 뒤집힌 쌍이다. 판정끼리는 둘 다 확정한 쌍에서 모두 같은 답을 냈다(6/6, 13/13, 7/7).

**판단: 우열을 말할 수 없다.** 사전 등록한 주 결과의 p 가 0.125 로 기준 0.05 를 넘는다. 방향은 셋 다 스킬 쪽이고 opus 판정 둘은 기준을 넘지만, 결과를 본 뒤에 주 결과를 유리한 쪽으로 옮기면 사전 등록을 한 뜻이 없어진다. README 의 「56쌍 49승 0패」 는 이 판단으로 바꾼다.

읽어 둘 것 셋이다.

1. **판정 모델이 결과를 가른다.** 같은 중립 기준에서 sonnet 은 14쌍 중 7쌍이 순서에 따라 뒤집혔고 opus 는 1쌍만 뒤집혔다. 갈린 쌍을 뺀 나머지에서 두 모델은 100% 같은 답을 냈다. 방향이 다른 것이 아니고 확신의 세기가 다르다.
2. **기준서의 감점 목록은 생각보다 덜 흔들었다.** 같은 opus 판정자에서 중립 기준 12승 1패와 옛 기준서 13승 1패는 거의 같다. L 절을 연 까닭이 「기준서가 스킬 규칙과 닮아 유리했다」 였는데, 적어도 이 표본에서는 그 효과가 크지 않다.
3. **표본이 작다.** 주 결과에서 확정된 쌍은 7개뿐이라 부호 검정이 기준을 넘기 어렵다. 쌍을 늘리면 같은 승률로도 기준을 넘을 수 있다. 늘리기 전까지는 결론을 세게 적지 않는다.

사람 블라인드 평가는 아직 돌리지 않았다. 그때까지 위 셋째 항목이 이 절의 가장 큰 한계다.

## M. output style 이 평소 답변의 세부를 떨어뜨리는가 (2026-09-14 신설)

H13 에서 상시 주입을 뺀 까닭이 세부 손실이었다. 같은 목적을 output style 로 하면 어떤지 다시 쟀다. 스타일은 기본 지시에 무엇을 더하지 않는다. 그 지시 자체를 바꾸고 매 요청에 실리므로 주입보다 개입이 크다.

**합격선은 돌리기 전에 정했다.** H13 이 쓴 것과 같다. 세부 11항목이 기준과 갈리지 않을 것, `delay` 기본값 언급이 0/8 이 아닐 것, 설계 답변 길이가 기준의 85% 이상일 것, 블라인드 문체 판정에서 기준에 지지 않을 것이다. 하나라도 떨어지면 싣지 않기로 했다.

**세부 보존.** 디바운스 질문(`always-on/prompts/01.txt`), claude-sonnet-5, 조건마다 표본 8.

| 조건        | 세부 11항목 | `delay` 기본값 | 한글 중앙값 | 기준과의 차이                    |
| ----------- | ----------- | -------------- | ----------- | -------------------------------- |
| A 없음      | 67/88 (76%) | 6/8            | 478         | 기준                             |
| O 스타일    | 75/88 (85%) | 8/8            | 512         | 순열검정 p=0.061, Fisher p=0.467 |
| B 옛 주입   | 66/88 (75%) | 0/8            | 396         | 순열검정 p=1.000, Fisher p=0.007 |
| N 중립 메모 | 66/88 (75%) | 6/8            | 443         | 순열검정 p=1.000, Fisher p=1.000 |

B 가 기본값을 여덟 번 모두 놓쳤다. H13 의 실패를 그대로 재현했으므로 이 장비가 실패를 잡아낸다는 뜻이다. 같은 장비에서 O 는 그 자리에서 8/8 이다.

**설계 과제.** claude-sonnet-5, 조건마다 표본 4, 과제 여덟. P 는 스타일을 켜지 않고 O 와 플래그만 같게 맞춘 짝이다.

| 묶음      | 개념 적중                          | 차이 95% CI              | 길이 중앙값      | 게이트       |
| --------- | ---------------------------------- | ------------------------ | ---------------- | ------------ |
| 짧은 설계 | P 141/160 (88%) 대 O 140/160 (88%) | [-7.9, +6.7]%p 차이 없음 | 2252자 대 2928자 | 비율 130% OK |
| 장문 설계 | P 184/192 (96%) 대 O 180/192 (94%) | [-6.7, +2.7]%p 차이 없음 | 3849자 대 4901자 | 비율 127% OK |

**블라인드 문체.** 프롬프트 넷을 순서를 바꿔 두 번씩 판정했다. 스타일 켬 3승, 끔 1승, 무승부 0.

**판단: 네 기준을 모두 넘겨 골라 켜는 스타일로 싣는다.** `force-for-plugin` 은 쓰지 않는다. 그 값은 사용자가 이미 고른 스타일을 덮는다.

**장비에서 고친 것.** `grade_design.py` 의 표본 유무 검사가 `any(OUT.glob(...) for pid in table)` 였다. `glob()` 이 돌려주는 제너레이터는 비어 있어도 참이라 이 검사는 늘 통과했고, 한쪽 조건에 표본이 없으면 `diff_ci` 가 0 으로 나눠 죽었다. `list()` 로 감싸 고쳤다. A·B 는 늘 양쪽에 표본이 있어 드러나지 않던 결함이다. 고친 뒤에도 옛 경로의 판정은 그대로다(짧은 설계 92% 대 92%, 장문 95% 대 89%).

**이 측정이 못 보는 것 다섯.**

1. 블라인드 쌍이 넷뿐이다. 3승 1패는 부호검정으로 p=0.625 라 우열의 근거가 아니다. 기준이 「지지 않을 것」이었고 그것만 넘겼다.
2. 세부 보존은 질문 하나에 표본 여덟이다. 과제를 넓히면 달라질 수 있다.
3. 설계 과제는 claude-sonnet-5 표본 넷으로 쟀다. 2026-09-11 기록은 claude-opus-5 표본 여덟이라 그 숫자와 직접 견주지 않는다.
4. 개념 적중은 합계로 갈리지 않았지만 O 가 더 많이 놓친 항목이 넷 있다. `D4 키 설계`(2 대 4), `D2 떼어내기`(0 대 2), `D2 격리 방식 선택`(1 대 3), `L2 로그 수집`(0 대 2)이다.
5. 스타일을 켠 경로가 배포 경로와 다르다. 프로젝트의 `.claude/output-styles/` 에 두고 켰다. `--plugin-dir` 로 올리는 길은 `--setting-sources` 를 `""`·`project`·`user,project` 로 바꿔도 세 번 다 적용되지 않았다. 설치한 사람이 `/config` 에서 이 스타일을 고를 수 있는지는 확인하지 못했다.

재현은 [`docs/experiments/output-style/`](experiments/output-style/README.md) 에 있다.

## 측정 결과 (2026-09-09)

| 기준                | 결과                                                    | 판정                          |
| ------------------- | ------------------------------------------------------- | ----------------------------- |
| A1 위반 검출        | 10/10                                                   | 통과                          |
| A2 정상 오탐        | 0/5                                                     | 통과                          |
| A3 실문서 오탐      | 1/143 (0.7%)                                            | 통과. 2026-09-10 재측정, 아래 |
| A4 분류 정확        | 10/10                                                   | 통과                          |
| B1 트리거 매칭      | 5/5                                                     | 통과                          |
| B2 코드 작업 오매칭 | 0/5                                                     | 통과                          |
| C1 처방 존재        | 8/8                                                     | 통과                          |
| C6 규칙집 커버리지  | quick 61항목 중 60 (개정 전 21)                         | 통과                          |
| C7 생성 단계 비교   | 블라인드 쌍대 12승 4패 대 옛 스킬 (p=0.038)             | 통과                          |
| C10 장문 결함       | 분량 미준수·것 구문 증가 둘 다 규칙 보강 후 해소          | 통과                          |
| C9 im-not-ai 대비   | 56쌍 49승 0패 (단문 28-0, 장문 21-0)                     | 통과 (우월)                   |
| C8 상시 규칙 회귀   | regress.sh 새 2승 옛 2승, 종료 0                        | 통과 (나빠지지 않음). 주입은 H13 으로 뺐다 |
| K9·K10 사람 글 오탐 | 0/32 (2023년 이전 문서)                                 | 통과                          |
| C3 자기 일관성      | 위반 0건                                                | 통과                          |
| D1 외부 호출        | 0건                                                     | 통과                          |
| D2 상시 비용        | 약 730 토큰 (스킬 설명 430 + 에이전트 297). 상시 규칙 816 은 H13 으로 뺐다 | 통과 (기준 1,600) |
| D3 변이 검출        | 15/15. 2026-09-10 새 판정 8종 추가 주입, 8/8            | 통과                          |
| D4 플러그인 로드    | loaded, 스킬 6종 + 에이전트 3종 (2026-09-10 격리 HOME 재확인) | 통과                          |
| D5 주입 안전성      | 주입을 뺐다 (H13)                                       | 해당 없음                     |
| C5 주입 효과        | 블라인드 21승 0패 3무 (24쌍). 문체만 본 값이다          | 통과. 세부가 빠지는 것이 H13 에서 드러나 주입을 뺐다 |
| E1 이상 입력        | 5종 전부 exit 0                                         | 통과                          |
| 회귀 테스트         | posttooluse 62/62 (2026-09-11)                          | 통과                          |

2026-09-10 재측정. 이 머신에 이 플러그인과 무관하게 쌓인 한국어 `.md` 143개(한글 20자 이상)를 `plugin/scripts/check.sh` 로 통째로 넣었다. 걸린 60개 중 59개는 2026년에 Claude 가 쓴 문서(CLAUDE.md, 구현 로그, QA 보고서. 대부분 K1 줄표)이고 사람이 쓴 문서는 1개다(외부에서 가져온 지식 정리 문서, K3·K8). 아래 10·11번을 고치기 전에는 사람이 쓴 문서 7개(4.9%)가 걸렸다.

상시 비용은 두 가지로 쟀다. 윤문 스킬이 있던 때의 스킬 설명 넷은 `claude -p "/context"` 가 내는 스킬별 표에서 70·70·90·60, 합계 약 290 이었다. `korean-writing` 설명을 글 작성 요청으로 넓히기 전에는 50 이었고 합계는 270 이었다. 상시 규칙은 설치된 플러그인을 그대로 두고 `KOREAN_WRITING_ALWAYS_ON_DISABLED=1` 로 껐다 켜며 `claude -p` 의 `usage` 입력 토큰 총량을 뺐다. 끄면 12,364, 켜면 13,026 으로 차이가 662 다. 스크래치 사본이 아니라 실제 훅 경로로 잰 값이다. 이 방법이 예전에는 실행마다 2,143토큰씩 흔들려 쓸 수 없었는데, `--strict-mcp-config` 로 MCP 도구 정의를 빼고 `--setting-sources ""` 로 사용자 설정을 빼니 값이 고정됐다.

윤문 파이프라인을 im-not-ai 에서 내장한 뒤 격리된 HOME 에 설치해 다시 재니, 스킬 설명은 70·230·60·70 으로 약 430 이고(슬래시 전용 스킬 둘은 목록에 실리지 않는다), 에이전트 셋이 297, 상시 규칙은 그대로 662 다. 합계 약 1,390 이다.

**항목별 추정은 환경에 따라 흔들린다 (2026-09-10 확인).** 같은 에이전트 파일인데 `claude -p "/context"` 의 Custom Agents 표가 격리 HOME 에서 102·107·88(합계 297), 사용자의 실제 HOME 에서 266·273·234(합계 773)로 나온다. 두 값 모두 여러 번 재현된다. 파일은 `diff` 로 같음을 확인했다. 항목별 추정치를 정밀한 값으로 인용하면 안 된다.

**재현되는 것은 상시 규칙 816 하나뿐이다.** 그것만 껐다 켤 스위치가 있어서 입력 토큰 총량을 빼는 방식으로 잴 수 있다. 스킬 설명과 에이전트는 끄고 켤 수단이 없어 `/context` 의 추정에 기댈 수밖에 없다. 격리 HOME 에 인증이 없어 그쪽에서는 API 호출 자체가 안 되므로(`terminal_reason: api_error`) 토큰 차이 측정도 못 한다.

그래서 D2 는 격리 HOME 기준 약 1,450 으로 둔다. 실제 사용 환경에서는 에이전트 몫이 더 크게 잡히므로 여유가 표보다 적다고 보는 편이 안전하다. 다음에 이 값을 다시 쓸 일이 있으면 어느 환경에서 잰 값인지 먼저 적는다.

**im-not-ai 원본과의 비교 (2026-09-10).** 원본을 커밋 9747f03 으로 받아 격리 HOME 에 설치하고 같은 표를 봤다.

| | 에이전트 수 | description 합계 | `/context` 토큰 |
| --- | --- | --- | --- |
| im-not-ai 원본 | 9 | 2,336자 | 899 |
| 이 저장소가 내장한 것 | 3 | 785자 | 297 |
| 싣지 않은 개발용 | 6 | 1,551자 | 약 600 |

개발용 여섯은 `korean-ai-tell-taxonomist`, `korean-translation-scholar`, `post-editese-metric-engineer`, `quick-rules-integrator`, `taxonomy-gap-analyzer`, `translationese-research-distiller` 다. 규칙집을 만들고 늘리는 데 쓰는 것이라 윤문을 돌릴 때는 부르지 않는다. 그래서 내장에서 뺐고, 그 결정이 상시 컨텍스트에서 약 600 토큰을 아낀다.

## 측정 중 고친 것

1. **한글 임계 100자 → 20자.** 실제 문단의 중앙값이 22자인데 임계가 100자라 92%를 걸러냈다. 첫 측정에서 검출률 0/10 이 나온 원인이다.
2. **K1 을 줄표 개수에서 삽입구 정규식으로.** 표 셀의 `| 항목 | PASS — 근거 |` 를 오탐했다. 표 행을 제외하고 `[^|\s]\s[—–]\s[^|\s]` 만 센다.
3. **K2 조사 확장 + `축` 오매칭 방지.** `레이어도` 를 놓쳤고, 압축·건축의 `축이` 를 잡을 뻔했다.
4. **K6 임계 1 → 2.** taxonomy D-8 의 처방이 "한 문서 1회 이하" 인데 1회에서 걸려 기준보다 엄격했다. (당시 규칙집은 claude-forge 판이었다. 지금의 im-not-ai 판에는 승패 의인화 항목이 없고, 처방은 SKILL.md 에 있다. 14번 참조.)
5. **K7·K8 신설.** 사물 의인화와 번역투 패턴이 아예 없어 정답 10건 중 4건을 놓쳤다.
6. **회귀 테스트 전면 재작성.** K1 을 정규식으로 바꾼 뒤 테스트를 갱신하지 않아 실패 상태였고, 그 탓에 변이 테스트가 거짓 통과했다.
7. **횟수 검증 도입.** 정규식 대안(`A|B|C`) 하나가 빠져도 다른 대안이 케이스를 살려 놓쳤다. 보고 횟수를 검증해 대안별 민감도를 얻었다.

8. **표 행 전체를 검사에서 제외.** 처음엔 K1(줄표)만 표를 뺐는데, README 의 전/후 교정표에서 '전' 칸의 나쁜 예가 K3·K7 로 걸렸다. 나쁜 예를 인용하는 문서가 그 예 때문에 걸리면 안 된다. 표는 산문이 아니므로 전 항목에서 뺐다. 회귀 케이스 1건 추가(27→28).

9. **영어가 대부분인 편집 안의 한국어 문단.** 편집분 전체의 한글 비율 30%로 잘라서, 영어 문서에 한국어 문단 하나가 섞인 편집은 검사하지 않았다. 한글 52자, 비율 0.04인 편집에 K6 2회와 K4가 있어도 exit 0이었다. 전체 비율이 미달이면 한글 비중 30% 이상인 줄만 모아 같은 기준으로 다시 본다. 영어 줄은 빠지므로 영어 산문의 줄표는 세지 않는다. 회귀 케이스 4건 추가(28→32).

10. **K7 에서 "죽다" 제외.** 서버가 죽다·프로세스가 죽다는 개발자의 일상어인데 사물 의인화로 잡았다. 실측 문서에서 "서버가 죽었", "훅이 죽어" 가 자연스러운 문맥으로 나왔고, 안내 문구조차 "죽는다" 를 정상 어휘로 들고 있었다. 문구도 "굳다·쓰러지다 같은 직역 비유 대신 멈춘다·죽는다처럼 사실대로" 로 고쳤다.

11. **K8 에서 "~에 대해"·"~를 통해" 횟수 판정 제외.** 실측 143개 중 3회 이상인 문서 10개가 전부 사람이 2022~23년에 쓴 블로그 글과 정리 노트였고, 2026년에 Claude 가 쓴 문서는 하나도 걸리지 않았다. 판별력이 없는 규칙은 사람의 글만 잡는다. 생성 단계의 회피는 스킬(품질 바)이 그대로 맡는다. 이중 피동·`가지고 있다`·`~에 의해` 는 남긴다. 정답 G10 은 여전히 잡힌다.

12. **제외 구간에 `~~~` 울타리 코드블록과 HTML 주석 추가.** 코드블록을 ``` 만 걷어내고 있었다. 회귀 케이스 4건 추가(36→40).

13. **줄표는 파일 전체로 판정.** 편집분만 세니 문단 하나씩 고치는 동안 쌓이는 줄표를 한 번도 못 잡았다. 실측 문서에서 줄표 34개, 66개, 207개짜리 파일이 그렇게 만들어졌다. 이번 편집에 줄표 삽입구가 하나라도 있으면 파일 전체(같은 제외·한국어 규칙)의 개수로 판정한다. 이번 편집에 없으면 파일에 아무리 많아도 잡지 않으니 소음은 그대로다. 회귀 케이스 3건 추가(40→43).

14. **규칙집과 압축 룰북을 im-not-ai 원본으로 교체 (2026-09-10).** 둘 다 claude-forge 를 거친 판이었다. taxonomy 는 forge 가 im-not-ai v2.0 에 D-8·D-9·J-3 상향을 더한 것이었고, quick-rules 는 forge 가 따로 만든 압축본이었다. im-not-ai 커밋 9747f036cdc2 (2026-09-06) 의 두 파일로 바꾸고 머리에 훅 제외 표시만 붙였다. 항목은 73개에서 84개(보류 1건 제외)가 됐다. 훅의 정규식은 손대지 않았으므로 판정은 바뀌지 않는다(회귀 43/43). 같은 디렉터리를 오늘 다시 재니 한국어 문서 169개 중 66개가 걸렸다(K1 58, K8 6, K3 5, K4 5, K7 5, K5 2, K2 0, K6 0). 아침의 143개·60개와 다른 것은 그 사이 문서가 늘었기 때문이고, 걸린 66개 중 6개는 이 저장소 `docs/experiments/` 의 실험 산출물이다. 승패 의인화·추상 구조어의 근거로 적혀 있던 출판사 편집부 일화는 forge 규칙집에만 있던 것이라 SKILL.md·README·훅 주석에서 빼고, 정답 데이터 G05·G06 을 근거로 적었다. `humanize-korean` 스킬은 im-not-ai 의 절차와 철칙을 바탕으로 다시 썼다.

15. **윤문 스킬과 규칙집 사본을 빼고 im-not-ai 를 플러그인 의존성으로 (2026-09-10).** 14번에서 원본으로 바꾼 사본을 다시 뺐다. 같은 이름의 스킬 둘이 뜨는 충돌과 사본 갱신 부담이 이유다. `plugin.json` 에 `dependencies: ["humanize-korean"]`, `marketplace.json` 에 im-not-ai 저장소를 커밋 9747f036cdc2 로 고정한 항목을 넣었다. 격리된 HOME 에서 `claude plugin install korean-writing` 이 "+ 1 dependency: humanize-korean" 으로 2.3.2 를 함께 설치했고, 캐시의 git HEAD 가 고정한 sha 와 같았으며, korean-writing 이 켜진 채로 humanize-korean 만 끄는 것은 막혔다. 훅과 회귀 테스트는 손대지 않았다(43/43, 20/20).

16. **윤문 파이프라인을 im-not-ai 에서 내장 (2026-09-10).** 15번의 의존성 방식은 `/humanize-korean:…` 이름이 그대로 남고 에이전트 아홉 종의 설명이 실렸다. 그쪽 이름을 숨기는 길을 셋 다 시험했다. 마켓플레이스 항목 `strict: false` 로 스킬을 골라 싣기는 "plugin.json 과 항목이 둘 다 구성 요소를 지정해 충돌" 오류로 로드가 실패했고, `skillOverrides` 는 문서대로 플러그인 스킬에 적용되지 않았으며, `permissions.deny` 는 실행만 막고 목록은 그대로였다(격리 HOME 의 터미널 화면을 캡처해 확인). 그래서 런타임 부분집합(스킬 3, 에이전트 3, 스크립트 9, 참조 파일)을 커밋 9747f036cdc2 에서 그대로 넣었다. 격리 HOME 에서 설치 뒤 `/humanize` 자동완성에 `/korean-writing:` 셋만 떴고, 사용자 설정을 뺀 실제 로그인으로 `/korean-writing:humanize` 에 AI 티를 몰아넣은 200자 문단을 넣으니 진단·윤문·마무리 검토 세 콜이 끝까지 돌았다(451초, 1.64달러, 변경률 39.0%, 등급 A-, 자체검증 6/6, 마무리 검토가 서법이 사라진 문장 하나를 되돌림). 기록은 `docs/samples/humanize-run.md` 에 있다. 훅과 회귀 테스트는 손대지 않았다(43/43, 20/20).

17. **제외 표시가 문서 일곱 종의 자기 검사를 통째로 꺼 놓고 있었다 (2026-09-10).** 훅은 `korean-writing: ignore` 라는 문자열이 편집분이나 파일 머리 4,000자 안에 있으면 검사를 건너뛰었다. 문제는 이 기능을 설명하는 문서가 그 문자열을 인용한다는 것이다. README·README.en·CLAUDE.md·CHANGELOG·CONTRIBUTING·CONTRIBUTING.en·SECURITY 일곱이 본문·표·코드블록에서 인용하고 있었고, `plugin/scripts/check.sh` 와 CI 의 「한국어 문서가 자기 훅을 통과하는가」 작업이 검사 대상 12개 중 7개를 아무것도 보지 않고 통과시켰다.

    확인은 표시 문자열만 무해하게 깨뜨린 사본을 만들어 다시 검사하는 것으로 했다. 일곱 문서 모두 그 상태에서도 K1~K8 을 통과했으므로 이 결함이 실제 위반을 가려 주고 있지는 않았다. 가려진 것은 위반이 아니라 검사 자체다.

    고친 방식은 줄 하나로 선 `<!-- korean-writing: ignore -->` 만, 그것도 파일 앞 열 줄 안에 있을 때만 지시로 보는 것이다. 실제로 끄려고 붙인 표시 여섯 개는 전부 1행에 줄 하나로 있어 그대로 산다. 문서가 이미 "파일 머리에 넣으라" 고 안내하고 있었으므로 코드를 문서에 맞춘 셈이다. 회귀 케이스 3건 추가.

18. **K9 부정 대구, K10 연결어미 뒤 쉼표 신설 (2026-09-10).** im-not-ai 규칙집에서 실측 판별력이 가장 큰 둘인데 훅에도 스킬에도 없었다. 임계는 이 머신에 쌓인 한국어 `.md` 205개로 정했다. 사람이 쓴 글과 Claude 가 쓴 글의 구분은 파일 수정 연도로 갈랐다(2023년 이전 32개, 2026년 173개). 거친 대리 지표이므로 한계로 적어 둔다.

    | 후보 임계 | 2023년 이전 | 2026년 |
    | --- | --- | --- |
    | 대구 2회+ | 0/32 | 40/173 |
    | 대구 3회+ (채택) | **0/32** | 30/173 |
    | 쉼표 4회+ 이면서 30%+ | 1/32 | 28/173 |
    | 쉼표 6회+ 이면서 30%+ (채택) | **0/32** | 21/173 |
    | 쉼표 6회+ 이면서 35%+ | 0/32 | 13/173 |

    규칙집의 대구 임계는 2회지만 훅은 3회로 뒀다. 훅이 스킬보다 느슨하다는 기존 방침대로다. 쉼표는 개수만 보면 긴 문서가 불리하고 비율만 보면 짧은 문서가 불리해 둘을 함께 넘을 때만 잡는다. 대구 정규식은 규칙집 구현을 따르되 조건절 `아니라면`·`아니라서` 를 뺐다. 가정이지 대구가 아니고, 실제로 생성물에서 "일요일 기준이 아니라면" 이 잡혔다. 문서 205개 판정은 그 변경으로 바뀌지 않는다. 회귀 케이스 9건 추가(43→55).

19. **`tools/measure.sh` 가 두 자리 코드를 세지 못했다 (2026-09-10).** 집계 정규식이 `K\d` 라 `K10` 줄이 어느 코드에도 안 잡혔다. 파일은 "걸린 파일" 로 세면서 코드별 표에서는 사라진다. K10 을 넣고 처음 돌린 측정에서 코드별 표에 K10 이 없어 발견했다. `K\d+` 로 고쳤다.

20. **저장소의 산문 문서를 새 판정에 맞춰 고쳤다 (2026-09-10).** 17번을 고치고 나니 README(K9 5회, K10 40회), EVALUATION(K10 16회), CHANGELOG(K10 16회), CONTRIBUTING(K9 3회)이 걸렸다. 연결어미 뒤 쉼표는 86개를 지웠고 부정 대구는 문서마다 둘 이하로 줄였다. 이 저장소의 문서가 이 머신 전체에서 연결어미 쉼표 비율이 가장 높았다(EVALUATION 57%, CHANGELOG 54%, README 46%).

21. **같은 규칙이 놓인 세 곳이 어긋났다 (2026-09-10).** 규칙 본문은 `SKILL.md` 가 정본이지만 사본이 둘 더 필요하다. 상시 규칙은 스킬이 안 뜨는 평소 답변을 맡고 `CLAUDE.md` 는 훅도 스킬도 전달되지 않는 서브에이전트를 맡는다(N1 실측). 플랫폼이 강요한 중복이라 없앨 수 없다. 이번에 대구와 연결어미 쉼표를 앞의 둘에만 넣고 `CLAUDE.md` 를 빠뜨려서 실측 최강 신호 둘이 서브에이전트에만 안 걸릴 뻔했다. 채워 넣고 `test_sessionstart.py` 에 세 곳 동기화 검사를 더했다(22→24건). 상시 규칙에서 대구 줄을 지워 보니 `빠진 항목: ['대구', '연결어미 뒤 쉼표']` 로 잡는다.

22. **이 문서의 한 절이 편집 중에 통째로 사라졌다 (2026-09-10).** 「측정 중 고친 것」 절 열여섯 건이 C9 절을 다시 쓰는 과정에서 지워졌고, `## D. 구조 건전성` 과 `## 상시 규칙을 넣으며 남겼던 것` 헤딩이 각각 두 번씩 붙어 있었다. README 를 실제 구현과 전수 대조하다가 "그렇게 바꾼 열세 건" 이라는 문장의 근거를 세려다 발견했다. git 에서 원문을 되살렸다. 문자열 치환으로 절을 갈아 끼울 때는 앵커가 남는지 헤딩 목록으로 확인한다.

23. **서브에이전트 훅의 출력 형식을 틀리게 알고 있었다 (2026-09-11).** 이 문서와 README 가 "서브에이전트 시작 훅은 돌지만 출력이 서브에이전트 컨텍스트로 가지 않는다" 고 적고 있었다. 맞는 말이지만 stdout 에만 해당한다. exit 0 의 stdout 을 컨텍스트로 받는 자리는 UserPromptSubmit·UserPromptExpansion·SessionStart·PostModelSwitch 넷이고 SubagentStart 는 거기 없다. JSON 의 `hookSpecificOutput.additionalContext` 로 실으면 닿는다. 같은 훅을 두 형식으로 돌려 갈랐다(아래 N1 재측정). 형식 하나 때문에 플러그인이 자리 하나를 통째로 포기하고 있었다.

24. **CI 의 자기 검사 목록이 낡아 문서 하나가 빠져 있었다 (2026-09-11).** 검사할 `.md` 를 워크플로에 손으로 열네 개 적어 두었는데 저장소가 쓴 `.md` 는 서른 개였다. 빠진 것 중 `docs/experiments/always-on/REPORT.md` 가 실제로 K3 와 K5 에 걸렸고 아무도 몰랐다. `plugin/scripts/check.sh --all` 을 만들어 `git ls-files` 로 열거하고 가져온 파일만 뺀다. 셸 스크립트의 실행 비트와 shellcheck 대상도 같은 이유로 손목록이었고 전수 열거로 바꿨다. shellcheck 을 안 거치던 실험 스크립트 여덟에서 SC2164 여덟 건과 SC2012 네 건이 나왔다.

25. **`plugin/scripts/check.sh` 가 끄기 스위치를 만나면 검사가 아니라 오류로 끝났다 (2026-09-11).** `KOREAN_WRITING_HOOK_DISABLED=1` 이 환경에 있으면 훅이 stdin 을 읽기 전에 끝나 파이프가 끊기고 `오류 (exit 120)` 이 나온다. 검사도 통과도 아닌 값이다. 이 스크립트를 부르는 것 자체가 검사하라는 뜻이므로 스위치를 걷어내고 부른다.

26. **가져온 파일 다수가 `Edit` 만 막혀 있었다 (2026-09-11).** `.claude/settings.json` 의 `ask` 규칙에 `Edit(/plugin/skills/humanize-korean/**)` 는 있는데 `Write(...)` 가 없었다. `plugin/agents/**`, `plugin/scripts/*.py`, `plugin.json`, `NOTICE.md`, `LICENSE` 도 같다. 통째로 덮어쓰는 경로가 열려 있었다. 짝을 채웠다.

27. **`hooks.json` 의 SessionStart matcher 에 `fork` 가 빠져 있었다 (2026-09-11).** `startup|resume|clear|compact` 만 있어 세션을 포크하면 규칙이 들어가지 않는다. 검사 훅의 파이썬은 `NotebookEdit` 을 받는데 matcher 는 보내지 않고, 보내더라도 경로가 `.ipynb` 라 `.md` 검사에서 걸러진다. 죽은 분기라 지웠다.

28. **KatFishNet 의 쉼표 포함률을 재 보고 넣지 않기로 했다 (2026-09-11).** 그 논문(ACL 2025)이 한국어 LLM 탐지에서 가장 센 지표로 꼽은 것이 문장부호이고, 이 저장소는 그중 연결어미 뒤 쉼표만 K10 으로 쓰고 있다. 남은 축인 「쉼표가 하나라도 든 문장의 비율」도 넣을 만한지 이 머신의 한국어 `.md` 로 쟀다. 논문은 에세이에서 사람 26.31% 대 LLM 61.03% 로 갈렸다고 보고한다.

    | 문서 | 표본 | 쉼표 포함률 중앙값 |
    | --- | --- | --- |
    | 2023년 이전 (사람이 쓴 것) | 18 | **41.5%** |
    | 2026년 (대부분 Claude 가 쓴 것) | 152 | **17.6%** |

    방향이 반대다. 어느 임계를 잡아도 사람 글이 더 많이 걸린다. 35% 에서 2023년 이전 11/18 대 2026년 11/152 이고, 50% 에서 6/18 대 3/152 이다. 이 저장소 문서를 빼고 다시 재도 41.5% 대 16.0% 로 같다. 논문의 값은 에세이 장르의 것이고 여기 코퍼스는 기술 문서라 옮겨 오지 않는다. 판별력이 뒤집힌 규칙은 사람의 글만 잡으므로 넣지 않는다. K8 의 `~에 대해` 를 뺐던 것과 같은 이유다.

    옮겨 오는 쪽은 K10 하나다. 쉼표 전반이 아니라 **연결어미 뒤** 로 좁혔기 때문에 남는다. 논문의 다른 두 축인 띄어쓰기 통계와 품사 n-gram 다양성은 형태소 분석기가 있어야 해서 표준 라이브러리만 쓰는 이 훅에서는 잴 수 없다.

## 상시 규칙을 넣으며 남겼던 것 (전부 끝남)

> 이 절이 잰 주입은 2026-09-11 H13 으로 뺐다. 기록으로 남긴다.

C5 를 넣을 때 못 잰 것 셋이다. 2026-09-10 에 전부 쟀고 결과를 아래에 적었다. 표는 무엇을 물었고 어떻게 답이 났는지다.

| #  | 무엇을 모르는가                                        | 재는 방법                                                                                                                                                    | 왜 중요한가                                                                                  |
| -- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| N1 | ~~서브에이전트~~ **끝남 (2026-09-11 뒤집힘)**                | SubagentStart 훅의 `additionalContext` 로 규칙이 닿는다. `plugin/hooks-handlers/subagentstart.sh` 가 맡는다                        | 아래에 실측을 적었다                                                                        |
| N2 | ~~긴 대화~~ **결론 남**                                  | 30턴 뒤에도 효과가 남는다. 아래에 실측을 적었다                                                                            | 긴 세션에서 없는 것과 같아지면 안 된다                                                       |
| N3 | ~~회귀 감시 수단~~ **끝남**                             | `docs/experiments/always-on/regress.sh`. 옛 규칙과 새 규칙으로 같은 프롬프트를 돌려 블라인드로 비교한다                     | 규칙을 고쳤을 때 나빠진 것을 잡을 수단이 없었다                                              |

**N1 은 끝났다 (2026-09-10).** SessionStart 훅이 낸 추가 컨텍스트는 서브에이전트에 전달되지 않는다. 같은 방식의 SessionStart 훅에 추측할 수 없는 토큰을 싣고 그 토큰을 모르는 지시문으로 서브에이전트에게 물었다. 부모 세션은 토큰을 그대로 냈고 서브에이전트는 두 번 다 `NONE` 을 냈다. 문구를 바꿔 두 번 돌린 결과가 같다. 훅이 돌고 출력만 안 가는 것인지 훅 자체가 안 도는 것인지도 갈랐다. 실행마다 파일에 한 줄을 남기는 SessionStart 훅을 matcher 없이 걸고 세 번 돌렸는데, 서브에이전트를 둘 띄운 실행에서도 기록이 한 줄이었고 `source` 가 `startup`, `agent_id` 가 비어 있었다. 서브에이전트를 위해서는 이 훅이 돌지 않는다. 출력이 갈 자리가 애초에 없다. 덤으로 서브에이전트가 늘어도 주입이 반복되지 않으니 추가 비용도 없다.

**N1 을 다시 열어 끝냈다 (2026-09-11).** 위 결론은 SessionStart 에 대해서는 그대로 맞다. 틀린 것은 그다음이다. SubagentStart 훅은 돌고, 출력을 JSON 으로 실으면 서브에이전트 컨텍스트에 들어간다. 같은 훅을 두 형식으로 각각 돌려 갈랐다. 훅만 아는 코드를 규칙 문단에 심고 그 코드를 프롬프트에 넣지 않은 채 서브에이전트에게 되읽게 했다.

| 훅이 낸 형식 | 서브에이전트가 답한 것 |
| --- | --- |
| stdout 으로 그냥 출력 | `SUB_INJECT=NONE` |
| `hookSpecificOutput.additionalContext` | `SUB_INJECT=ZULU9143` (심은 코드 그대로) |
| SessionStart 의 stdout | `SUB_SESS=NONE` (상속되지 않는다) |

공식 문서와 맞는다. exit 0 의 stdout 을 컨텍스트로 받는 자리는 UserPromptSubmit·UserPromptExpansion·SessionStart·PostModelSwitch 넷이고 SubagentStart 는 그 목록에 없다.

플러그인에 `plugin/hooks-handlers/subagentstart.sh` 를 넣고 `--plugin-dir` 로 격리해 띄워 다시 확인했다. 서브에이전트에게 규칙 문단의 항목 수를 세게 하니 `SUB_RULES=10` 이 나왔고 `always-on.md` 의 항목 수와 같다. 규칙 파일은 세션 시작 훅과 같은 것을 읽으므로 두 벌로 갈라지지 않는다. `test_subagentstart.py` 가 그 동일성까지 본다.

**비용은 서브에이전트마다 816 토큰이다.** 세션당 한 번인 상시 주입과 달리 서브에이전트를 띄운 수만큼 곱해진다. 그래서 이 훅만 끄는 스위치를 따로 뒀다(`KOREAN_WRITING_SUBAGENT_DISABLED=1`, 플러그인 설정 `subagent_rules`). 이 플러그인의 윤문 에이전트 셋은 규칙을 본문에 이미 갖고 있어 훅이 `agent_type` 을 보고 건너뛴다.

**한계.** 서브에이전트에 스킬 목록은 여전히 가지 않는다. 규칙은 닿아도 서브에이전트가 스스로 `korean-writing` 스킬을 열지는 못한다.

이어서 세 경로를 더 쟀다. 서브에이전트 시작 훅의 stdout 도 전달되지 않는다(훅이 돌았다는 것은 같은 명령에서 파일에 남긴 기록으로 확인했다). 플러그인 스킬 목록도 전달되지 않는다(`korean-writing` 이 목록에 있느냐는 물음에 `NO`). 전달되는 것은 `CLAUDE.md` 하나뿐이다(사용자 지침의 문장을 그대로 인용해 돌려줬다).

플러그인이 서브에이전트에 넣을 경로가 없다. 그래서 범위 밖으로 두고 맡고 싶은 사람에게 `CLAUDE.md` 한 줄을 안내한다. 이 저장소의 `CLAUDE.md` 에 같은 줄을 넣어 본보기로 뒀다.

**N2 도 끝났다 (2026-09-10).** 30턴을 채운 뒤에도 주입 조건이 이긴다.

한 세션에 채움 질문 30턴과 긴 답변 넷을 쌓아 컨텍스트를 3만에서 4만 토큰까지 키운 뒤, 그 대화에서 한 번도 안 나온 프롬프트 넷(02·06·07·12)을 물었다. 주입 조건과 기준 조건을 같은 깊이에서 나란히 돌리고 순서를 바꿔 두 번씩 블라인드로 판정했다.

| 측정                     | 기준     | 주입    |
| ------------------------ | -------- | ------- |
| 블라인드 쌍대 승 (4쌍)   | 0        | **4**   |
| 무승부                   | 0        | 0       |
| 훅 검사기 적중           | 0        | 0       |
| 굵게 `**` 사용           | 29       | **7**   |
| 불릿 줄                  | 14       | **5**   |

네 쌍 모두 두 순서에서 같은 답이 나왔다. 주입문이 대화 맨 앞에 있어도 30턴 뒤까지 힘이 남는다. 주입 조건의 세션에 마지막으로 물어보니 규칙 첫 줄을 그대로 냈고 기준 조건은 `NONE` 을 냈다.

**설계에서 한 번 틀렸다.** 처음에는 같은 프롬프트를 대화 앞과 뒤에서 각각 물어 비교하려 했다. 그러면 뒤의 답이 앞의 답을 그대로 되풀이한다. 기준 조건 네 건이 글자까지 같았다. 앞뒤 비교를 버리고 같은 깊이에서 두 조건을 나란히 놓는 방식으로 바꿨다.

**한계.** 프롬프트가 넷이고 표본이 1이다. 채움 질문이 한 줄짜리 산수라 실제 작업 대화와 다르다. 30턴에서 봤고 그보다 긴 대화는 재지 않았다.

**N3 도 끝났다 (2026-09-10).** `docs/experiments/always-on/regress.sh` 를 만들었다. 옛 규칙과 새 규칙으로 같은 프롬프트를 각각 생성하고 어느 쪽이 새 규칙인지 모르는 판정자에게 순서를 바꿔 두 번 물어 이긴 쪽을 센다. 새 규칙이 진 쌍이 더 많으면 종료 코드 1 이다.

채택 시점의 답변을 얼려 두고 대조하는 방식은 버렸다. 생성 비용이 절반이지만 모델이 바뀌면 그 차이까지 회귀로 잡힌다. 두 규칙을 같은 시각 같은 모델로 돌리면 규칙 차이만 남는다.

프롬프트 넷 기준으로 생성 8회와 판정 8회, 3~4달러다. LLM 을 부르므로 CI 에는 넣지 않고 릴리스 전에 사람이 한 번 돌린다. 높임 규칙을 넣기 전 판과 지금 판을 프롬프트 07 로 비교해 실제로 돌려 봤고 새 규칙이 이겨 종료 코드 0 이 나왔다.

C5 실험 자체의 한계는 그 절에 적어 두었다. 판정 모델과 생성 모델이 같고 표본이 2다. 기준선 환경에서 사용자 설정을 뺐다.

## 재현

```bash
python3 tests/test_posttooluse.py     # 회귀 62건
tools/measure.sh ~/Documents                 # 실제 문서 뭉치의 오탐 측정
docs/experiments/skill-vs-imnotai/run.sh       # 스킬 대 im-not-ai 블라인드 판정
docs/experiments/detail-retention/run.sh       # 훅 안내대로 고친 글에서 정보가 사라지는가 (I1)
```

앞의 둘은 LLM 을 부르지 않아 언제든 돌립니다. 뒤의 둘은 부르므로 릴리스 전에 한 번 돌립니다. C7·C9 의 판정은 `skill-vs-imnotai` 가 그대로 재현합니다. 설치본을 `plugin/` 으로 나눈 뒤 2026-09-11 까지 이 스크립트가 옛 경로를 읽어 스킬과 규칙집 없이 판정이 돌았습니다. 파일을 읽지 못해도 멈추지 않고 「회귀 없음」 을 냈습니다. C7·C9 는 나누기 전인 2026-09-10 에 잰 것이라 영향이 없습니다. 경로를 고쳤고 필요한 파일이 없으면 LLM 을 부르기 전에 종료 코드 2 로 멈추게 했습니다.

실문서 오탐은 분기마다 다시 잰다. `tools/measure.sh` 로 실제 문서가 쌓인 디렉터리를 돌리고 걸린 파일을 사람이 쓴 글과 Claude 가 쓴 글로 나눠 이 문서에 날짜와 함께 적는다. 사람이 쓴 글의 비율이 1% 를 넘으면 그 코드의 규칙을 다시 본다. 2026-09-10 측정은 이 방법으로 했다(이 저장소의 문서를 포함하면 154개 중 60개, 사람 글 1개).

정답 데이터는 `tests/ground-truth.json`(위반 10건)과 `clean.json`(정상 5건)이다.
실제로 생성됐던 어색한 문장에서 가져왔다. 합성 예문이 아니다.
